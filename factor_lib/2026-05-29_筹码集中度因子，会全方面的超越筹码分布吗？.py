"""筹码集中度因子，会全方面的超越筹码分布吗？

自动生成自 ../sources/general/articles/2026-05-29_筹码集中度因子，会全方面的超越筹码分布吗？.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2026-05-29_筹码集中度因子，会全方面的超越筹码分布吗？/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 筹码集中度因子，会全方面的超越筹码分布吗？
# 日期    : 2026-05-29
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247496938&idx=1&sn=022469e0a2d42559acf19d5e4a206e05
# 本地原文: ../sources/general/articles/2026-05-29_筹码集中度因子，会全方面的超越筹码分布吗？.md
# 本地图片: ../sources/general/articles/images/2026-05-29_筹码集中度因子，会全方面的超越筹码分布吗？/  (共 17 张)
# 段落识别: PARTIAL  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **筹码集中度**
#
# 在中信建投陈升锐和姚紫薇两位老师在2025年8月25日发布的研报《筹码分布因子系统构建》中，一共有四个主题的因子。
#
# 此前，笔者复现了第一个主题，筹码分布。在这个主题中，表现最好的是偏度因子。碰巧的是，笔者遇到了一位同样复现过这篇研报的大佬，在他的复现结果中，也是偏度表现最好。
#
# 同时，他还给了笔者一个信息，其他的因子都不太行。
#
# 所以，在笔者复现筹码集中度因子的时候，并没有抱任何期待。只是想着代码的改动不是很大，睡觉前改一下代码然后跑一个晚上就好了。
#
# **计算公式和代码**
#
# 这一次，主要是复现筹码集中度因子。
#
# 但是，由于在筹码分布因子中，笔者有三个计算错误了，在这次的代码中对他们进行了修正。
#
# 同时，筹码分布的因子，笔者认为可以分为两类，第一类比较简单，也就是这篇文章介绍的。另外一类稍微复杂一点，还要按照涨跌停的幅度进行分类，所以就下次介绍了。
#
# **1**
#
# **计算公式**
#
# **2**
#
# **代码**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# 在研报中关于这六个因子的逻辑介绍是这样的。
#
# 筹码集中度越高，往往对于出货阶段，市场炒作明显，未来表现可能不佳。
#
# 这就意味着，这是一个负向因子。
#

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 筹码集中度相关的六个因子，他们在IC上的表现是差不多的，这从相关性的热力图中也可以看出来。这六个因子之间的相关性，最低的也有0.95。
#
# 至于前三个因子，它们属于上一篇文章中的遗留物，不是本文介绍的重点。不过，值得一提的是，ckdp这个因子的IC是这篇研报到目前为止最高的。
#
# 由于本文介绍的这六个因子相关性很高，所以笔者选择了chip\_dirst1进行结果展示。同时，这个因子在进行低频化的时候，用均值低频化是全方面优于标准差的。
#
# ## 因子评价
# 这个因子怎么说呢？
#
# 确实不咋地，感觉各方面都不如筹码分布中的偏度因子了。
#
# 如此看来，这次尝试失败了。
#
# 没办法，笔者就只能厚着脸皮，来问各位大佬要要赞关注推荐和分享了。
#

# ============================================================
# 本地图片清单（共 17 张）
# ============================================================
# 001.png  <-  计算公式
# 002.png  <-  因子逻辑
# 003.png  <-  因子评价
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  IC分析
# 010.png  <-  回归分析
# 011.png  <-  回归分析
# 012.png  <-  换手率分析
# 013.png  <-  换手率分析
# 014.png  <-  收益分析
# 015.png  <-  收益分析
# 016.png  <-  收益分析
# 016.png  <-  因子评价

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def cal_factors(self, code, close, volume, factor, tr):
    data = pd.concat([close, volume], axis=1)
    data.columns = ['close', 'volume']
    data = data.groupby('close', as_index=False, group_keys=False).sum()
    last_data = self.last_chip.get(code, None)
    if last_data is None:
        data.iloc[:, -1] *= tr
    else:
        last_data['close'] /= factor
        data = data.merge(last_data, how='outer', on=['close'])
        data = data.fillna(0)
        data['volume'] = data.iloc[:, -1] * (1 - tr) + data.iloc[:, -2] * tr
        data = data[['close', 'volume']]
    chip_high = data['close'].max()
    chip_low = data['close'].min()
    diff = chip_high - chip_low
    close = close.iloc[-1]
    weight = data['volume'] / data['volume'].sum()
    mean = np.average(data['close'], weights=weight)
    q = data['close'].quantile([0.05, 0.95, 0.15, 0.85, 0.25, 0.75])
    q_90 = q.iloc[1] - q.iloc[0]
    q_70 = q.iloc[3] - q.iloc[2]
    q_50 = q.iloc[5] - q.iloc[4]
    chip_distri1 = 2 * q_90 / (q.iloc[1] + q.iloc[0])
    chip_distri2 = q_90 / chip_high
    chip_distri3 = 2 * q_70 / (q.iloc[3] + q.iloc[2])
    chip_distri4 = q_70 / chip_high
    chip_distri5 = 2 * q_50 / (q.iloc[5] + q.iloc[4])
    chip_distri6 = q_50 / chip_high
    data.iloc[:, 0] *= factor
    self.last_chip[code] = data.copy()
    return ((close - mean) / diff, chip_high / chip_low - 1, (mean - chip_low) / diff,
            chip_distri1, chip_distri2, chip_distri3, chip_distri4, chip_distri5, chip_distri6)

# (作者注) 代码其实也不复杂，就是按照公式来计算。
#
# (作者注) 第20-29行，就是公式的代码表示。

