"""量价关系的高频乐章，连续五年IC绝对值超过0.1！

自动生成自 ../sources/general/articles/2025-06-23_量价关系的高频乐章，连续五年IC绝对值超过0.1！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2025-06-23_量价关系的高频乐章，连续五年IC绝对值超过0.1！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 量价关系的高频乐章，连续五年IC绝对值超过0.1！
# 日期    : 2025-06-23
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247489336&idx=1&sn=2f35188bae2670bfbbb4e9062d3f48f6
# 本地原文: ../sources/general/articles/2025-06-23_量价关系的高频乐章，连续五年IC绝对值超过0.1！.md
# 本地图片: ../sources/general/articles/images/2025-06-23_量价关系的高频乐章，连续五年IC绝对值超过0.1！/  (共 23 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# 本文要复现的因子来自方正证券2020年2月27日发布的研报《市场微观结构剖析系列6——量价关系的高频乐章》，作者是朱定豪和严佳炜两位老师。
#
# 在这篇研报中，主要介绍了四个因子。
#
# 分别是，收盘价和成交额的相关性；对数收益率的绝对值和成交额的相关性；滞后一期的成交额与对数收益率绝对值的相关性；滞后一期对数收益率绝对值与成交额的相关性。
#
# 本文复现的是最后一个，滞后一期对数收益率绝对值与成交额的相关性。
#
# 看到这里，各位大佬肯定会有疑惑，不就是计算一个相关性吗？有什么复杂的呢？
#
# 因为，这里的成交额不是直接使用的，而是需要进行一次z-score标准化的。同时，这里的z-score标准化不是截面的，也不是同一天时序上的，而是过去20天同一分钟的成交额进行z-score标准化。
#
# 对于这个因子，其背后的逻辑是：分钟成交额在日内呈U型或W型分布，与收益率分布的不对称会影响相关性的计算，对成交额做标准化可以捕捉成交额的相对波动；若存在较多收益为0的样本也会使系数偏高，需将其剔除；修正后的因子能捕捉更真实纯粹的量价异动，因子表现大幅提升。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 收益分析
# 不去掉收益率为0的情况
#
# 这种情况下，用标准差合成的因子只有1年的IC绝对值超过了0.1，分层回测看起来两者相差不是太大。如果看因子值最低的一组和最高的一组的多空收益的话，均值合成的效果更好。
#
# 因此，仅展示一下均值合成的结果。
#
# ## 收益分析
# 总结：从因子评价结果来看，不去掉收益率为0的情况更好。同时，用均值合成月度因子的表现也更佳。
#

# ============================================================
# 本地图片清单（共 23 张）
# ============================================================
# 001.png  <-  (文章开头)
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  回归分析
# 009.png  <-  回归分析
# 010.png  <-  换手率分析
# 011.png  <-  换手率分析
# 012.png  <-  收益分析
# 013.png  <-  IC分析
# 014.png  <-  IC分析
# 015.png  <-  IC分析
# 016.png  <-  IC分析
# 017.png  <-  IC分析
# 018.png  <-  IC分析
# 019.png  <-  回归分析
# 020.png  <-  回归分析
# 021.png  <-  换手率分析
# 022.png  <-  换手率分析
# 023.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    if idx < 20:
        return pd.DataFrame({})
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]

    # 加载当日分钟数据
    full_path = os.path.join(self.file_pth, file_name)
    data = BaseDataLoader.load_data(full_path, fields=['close']).to_dataframes()
    ret = np.abs(np.log(1+data['close'].pct_change().shift(1)))

    amt = []
    for i in range(idx-20,idx+1):
        file_name = self.files[i]
        full_path = os.path.join(self.file_pth, file_name)
        amt.append(BaseDataLoader.load_data(full_path, fields=['turnover']).to_dataframe('turnover'))
    amt = pd.concat(amt)
    amt['minute'] = amt.index.hour * 60 + amt.index.minute
    amt = amt.groupby('minute', as_index=False, group_keys=False).apply(self.z_score)

    res = ret.corrwith(amt)
    res.name = pd.to_datetime(date_str) + timedelta(hours=15)
    return res

def z_score(self, group):
    group = group.sub(group.mean(axis=0), axis=1).div(group.std(axis=0), axis=1)
    group = group.iloc[[-1]]
    return group.drop(columns=['minute'])

# (作者注) 第8-10行，计算滞后一阶的对数收益率的绝对值。
#
# (作者注) 第12-17行，读取过去21个交易日的成交额数据。
#
# (作者注) 第18行，计算当前交易时间是当天的第几分钟，用于进行同分钟数据的z-score标准化。
#
# (作者注) 第19行，对分钟相同的数据进行聚合，然后标准化。
#
# (作者注) 这里需要注意的是，笔者计算相关性的时候并没有去掉收益率为0的情况，因为在因子评价的时候发现不去掉效果更好。
#
# (作者注) 去掉收益率为0的情况
#
# (作者注) 这种情况下，如果用标准差合成的话，IC会大幅下降，绝对值最高的一年也就勉强超过了0.07。但是，分层回测却展现了不错的单调性。
#
# (作者注) 上面这个分层回测的图是基于标准差合成的，下面完整的因子评价结果都是基于均值合成的。

