"""协同价差，跑了三个通宵，最后用18个小时跑了1800天的数据，结果居然这样！

自动生成自 ../sources/caochunxiao/articles/2025-05-01_协同价差，跑了三个通宵，最后用18个小时跑了1800天的数据，结果居然这样！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/caochunxiao/articles/images/2025-05-01_协同价差，跑了三个通宵，最后用18个小时跑了1800天的数据，结果居然这样！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: caochunxiao
# 公众号  : 量化拯救散户
# 标题    : 协同价差，跑了三个通宵，最后用18个小时跑了1800天的数据，结果居然这样！
# 日期    : 2025-05-01
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247488315&idx=1&sn=ee4dafee752a2e95d594751aef70ea59
# 本地原文: ../sources/caochunxiao/articles/2025-05-01_协同价差，跑了三个通宵，最后用18个小时跑了1800天的数据，结果居然这样！.md
# 本地图片: ../sources/caochunxiao/articles/images/2025-05-01_协同价差，跑了三个通宵，最后用18个小时跑了1800天的数据，结果居然这样！/  (共 11 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:27

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# 本文将继续复现上一篇文章中提到的研报中的协同价差因子。
#
# 本来这个因子应该是在成交量协同后面发布的，但是因为代码不规范跑了三个通宵才有了结果。
#
# 第一个通宵，凌晨1点多跑完了成交量协同因子。因为第一次跑的成交量协同因子在计算协同成交量的时候没有减去自己的成交量占比，于是重跑了一下。
#
# 睡梦中有人喊笔者，代码跑完了可以跑下一个了，在两次呼唤之后，笔者醒来了。本来是打算从2017年开始跑的，可是参数改错了地方，还是从2004年开始跑了。运行之后躺到床上，本想设置500天保存一次的，结果不想二次起床了，主要还是想着只跑2000多天，应该5个多小时也能跑完了。
#
# 结果第二天起来发现，参数设置错了，跑了10个小时才跑到3666天。因为是多核计算，没办法对其他的因子进行评价，为了不影响断更，直接关掉了。
#
# 第二次，设置了从2017年11月开始跑，设置了200天保存一次。结果保存的时候是覆盖不是追加，一晚上白跑。真是代码不规范，醒来泪两行啊！这也是笔者一直以来存在的问题，代码不规范，没有注释各种重复代码，看来还是教训不够。
#
# 好了，题外话说得有点多了，下面进入正文吧！
#
# 这个因子的计算有点小复杂了。
#
# 第一步，对每个标的，计算三个符号。
#
# 这三个符号是最近1分钟收益率符号，当前1分钟收益率相对前5分钟收益率（不包括当前分钟）的变化符号，当前1分钟成交量相对前5分钟成交量（不包括当前分钟）的变化的符号。
#
# 若最近1分钟收益率、当前1分钟收益率相对前5分钟收益率变化、当前1分钟成交量相对前5分钟的成交量变化为零，则它们的符号根据当前分钟的收盘价相对前5分钟（不包括当前分钟）均价的变化的符号为准，若该符号仍为零，则按照当前分钟的收盘价相对前一日收盘价变化的符号为准。
#
# 这样，对于一个标的每分钟有三个符号。
#
# 将该标的与其他所有标的比较，将同一时间同一种类符号相同的次数累加，得到该标的与其他标的的协同次数。
#
# 第二步，取当天与其协同次数最多的30只股票作为其协同效应股票，将该股票当天收益率减去这30只股票的平均收益率，得到相对协同股票的超额收益率。
#
# 虽然，看起来只有两步，但是通过代码实现就有那么一点点复杂了。
#
# 在实现中，笔者一共定义了三个方法，我们一个一个来看。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 收益分析
# 总结：这个因子和研报的差距还是有一点点的，可能是因为没有去掉ST的原因，也可能是没有理解到研报的精髓吧，但是我想我应该不会再复现它了吧。用标准差合成月度因子IC会高一点点，但是分层回测会更差，所以就展示了一下用均值合成的记过。
#
# 最后，祝各位读者五一快乐！也希望大家多多点赞推荐和关注，谢谢大家！
#

# ============================================================
# 本地图片清单（共 11 张）
# ============================================================
# 001.png  <-  IC分析
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  回归分析
# 008.png  <-  回归分析
# 009.png  <-  换手率分析
# 010.png  <-  换手率分析
# 011.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 3 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]

    if idx == 0:
        return
    else:
        last_df = self.last_close.iloc[[idx-1]]

    # 加载当日分钟数据
    full_path = os.path.join(self.file_pth, file_name)
    data = BaseDataLoader.load_data(full_path, fields=['close', 'volume'])
    last_df = last_df.reindex(columns=data.codes)
    dfs = data.to_dataframes()

    ret = dfs['close'].pct_change(1)
    ret_ma5 = ret - ret.rolling(5).mean().shift(1)
    vol_ma5 = dfs['volume'] - dfs['volume'].rolling(5).mean().shift(1)
    close_diff = dfs['close'] - dfs['close'].rolling(5).mean().shift(1)
    close_diff_last = dfs['close'].sub(last_df.iloc[0], axis=0)

    self.ret_daily = dfs['close'].iloc[-1] / last_df.iloc[-1] - 1
    self.ret_dir = self.cal_flag(ret, close_diff, close_diff_last)
    self.ret_ma5_dir = self.cal_flag(ret_ma5, close_diff, close_diff_last)
    self.vol_ma5_dir = self.cal_flag(vol_ma5, close_diff, close_diff_last)
    res = Parallel(n_jobs=-1, verbose=10)(
        delayed(self.cal_factors)(code)
        for code in data.codes
    )
    res = pd.Series(data=res, index=data.codes.tolist(), name=pd.to_datetime(date_str) + timedelta(hours=15))
    return res

# (作者注) 这里需要用到前一天的收盘价，所以多了第5-8行的代码。
#
# (作者注) 第14行之前的代码都是用来加载数据的。
#
# (作者注) 第16行，计算收益率。
#
# (作者注) 第17行，计算收益率相对于前5分钟的变化。
#
# (作者注) 第18行，计算成交量相对于前5分钟均值的变化。
#
# (作者注) 第19行，计算收盘价相对于5分钟前的变化。
#
# (作者注) 第20行，计算收盘价相对于昨日收盘价的变化。
#
# (作者注) 第22行，计算标的当日的收益率，在最后计算超额收益率的时候会用到。

# --- 代码块 2 ---
@staticmethod
def cal_flag(data, mask_data1, mask_data2):
    data_dir = data > 0
    mask = (data == 0) & (mask_data1 != 0)
    data_dir[mask] = mask_data1 > 0
    mask = (data == 0) & (mask_data1 == 0) & (mask_data2 != 0)
    data_dir[mask] = mask_data2 > 0
    return data_dir

# (作者注) cal\_flag方法就是计算数据的符号，拿收益率举例。
#
# (作者注) 第3行，就是对收益率大于0的置为1，其他为0。
#
# (作者注) 第4行，找到mask\_data1不为0的位置。在这里其实就是当前收盘价和5分钟之前收盘价之差。
#
# (作者注) 第5行，将第4行找到的位置中的数据按照，mask\_data1来填充其符号。如果mask\_data1大于0，就为1，否则继续为0。
#
# (作者注) 第6-7行，就是将4-5行的mask\_data1换成了mask\_data2。在这里，是当前收盘价与前一日的收盘价之差。

# --- 代码块 3 ---
def cal_factors(self, tar):
    res = 0
    for key in ['ret_dir', 'ret_ma5_dir', 'vol_ma5_dir']:
        tmp_data = self.__getattribute__(key)
        tar_data = tmp_data[tar]
        tmp_data = tmp_data.drop(columns=[tar])
        res += tmp_data.mul(tar_data, axis=0).sum()
        res += (1 - tmp_data).mul(1 - tar_data, axis=0).sum()
    res.sort_values(inplace=True, ascending=False)
    res = res.iloc[:30]
    ex = self.ret_daily.loc[tar] - self.ret_daily.reindex(res.index).mean()
    return ex

