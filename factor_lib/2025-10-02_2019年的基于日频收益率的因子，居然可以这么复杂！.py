"""2019年的基于日频收益率的因子，居然可以这么复杂！

自动生成自 ../sources/general/articles/2025-10-02_2019年的基于日频收益率的因子，居然可以这么复杂！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2025-10-02_2019年的基于日频收益率的因子，居然可以这么复杂！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 2019年的基于日频收益率的因子，居然可以这么复杂！
# 日期    : 2025-10-02
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247490479&idx=1&sn=27e6b2c15d637581a6f0e0054c6c8405
# 本地原文: ../sources/general/articles/2025-10-02_2019年的基于日频收益率的因子，居然可以这么复杂！.md
# 本地图片: ../sources/general/articles/images/2025-10-02_2019年的基于日频收益率的因子，居然可以这么复杂！/  (共 11 张)
# 段落识别: PARTIAL  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **点击蓝字 关注我们**
#
# 本文笔者将复现中信建投2019年发布的研报《基于市场羊群效应的股票alpha研究》。
#
# 这篇研报发表在2019年，虽然它是一个基于日频数据计算的因子，但是这个因子的计算可以说是一点也不简单。笔者用i9开了16个进程来计算都花费了70多分钟。
#
# 没想到啊，6年前的因子就这么卷了。
#
# 那么一个复杂的因子会有更好的效果吗？

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# 这个因子的计算步骤有四步。
#
# 第一步，计算当前标的和其他所有标的过去120个交易日的涨跌幅相关系数。
#
# 第二步，选取相关系数最大的9只股票，加上当前标的一共10只股票构成一个板块。
#
# 第三步，以当前标的为中心，计算板块过去120个交易日的横截面绝对偏差CSAD。即abs（板块所有标的收益率-当前标的收益率），然后在截面上取均值。
#
# 第四步，分别计算当前标的过去20个交易日的CSAD标准差和收益率标准差的比值，过去120个交易日的CSAD标准差和收益率标准差的比值，用短周期的除以长周期的就得到了最后的因子。
#
# 文字确实有点难理解，所以结合代码来看一下吧。
#

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 在这四个因子当中，只有CSAD的标准差这个因子的表现稍微好一点，其他的三个因子都比较一般。
#
# 它们的IC绝对值偏低，最高的一年也就刚刚超过0.06。
#
# 它们的分层回测就更不用提了，可以说是从头相交到尾。
#
# 所以下面仅仅展示一下CSAD
#
# ## 总结
# 对于这个复杂的因子，笔者只能说“想法很好，效果一般”。
#
# 从IC上来看，这个因子还是不错的，其中有一年的IC绝对值甚至超过了0.12。但是，它在分层回测上的表现来看，这又是一个空头因子。
#

# ============================================================
# 本地图片清单（共 11 张）
# ============================================================
# 001.png  <-  IC分析
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  回归分析
# 008.png  <-  回归分析
# 009.png  <-  换手率分析
# 010.png  <-  换手率分析
# 011.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 2 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    ret = self.ret.iloc[idx - 120:idx].dropna(how='all', axis=1)
    ret = ret.dropna(thresh=20, axis=1)
    ret = ret.loc[:, [col for col in ret.columns if (ret[col] != 0).sum() >= 20]]
    corr = ret.corr()
    day = ret.iloc[[-1]].index.tolist()[0]
    res = []
    for code in corr.columns:
        res.append(self.__process_single_day__(corr[code], ret, code))
    res = pd.DataFrame(data=res, index=corr.columns.tolist(),
                       columns=['ratio_20', 'ratio_120', '20_div_120', 'std_21'])
    res['datetime'] = day
    return res

# (作者注) 第2行代码，获取过去120个交易日的收益率信息。
#
# (作者注) 第3行代码，去掉收益率为nan的交易日超过20的标的。
#
# (作者注) 第4行代码，去掉收益率为0的交易日超过20的标的。
#
# (作者注) 第5行代码，计算相关性。
#
# (作者注) 第8-9行，循环调用\_\_process\_single\_day\_\_方法计算每个标的的因子。

# --- 代码块 2 ---
@staticmethod
def __process_single_day__(corr, ret, code):
    corr = corr.dropna().sort_values()
    if len(corr) < 10:
        return [np.nan] * 4

    corr = corr.iloc[-10:]
    sector_ret = ret[corr.index.tolist()]
    csad = (sector_ret - sector_ret[code].values.reshape(-1, 1)).abs().mean(axis=1)
    csad.sort_index(inplace=True)
    sigma_20 = csad.iloc[-20:].std() / sector_ret[code].iloc[-20:].std()
    sigma_120 = csad.std() / sector_ret[code].std()
    return [sigma_20, sigma_120, sigma_20 / sigma_120, csad.iloc[-21:].std()]

# (作者注) 第3行代码，对于相关性不为nan的数量少于10的标的直接返回nan。
#
# (作者注) 第7行代码，获取板块内的10个标的。
#
# (作者注) 第8行代码，获取板块内10个标的收益率数据。
#
# (作者注) 第9行代码，执行步骤中的第三步，计算csad。
#
# (作者注) 最后，笔者计算了四个因子，分别是过去20个交易日的CSAD标准差和收益率标准差的比值，过去120个交易日的CSAD标准差和收益率标准差的比值，用短周期的除以长周期的就得到的最后的因子以及CSAD的标准差。

