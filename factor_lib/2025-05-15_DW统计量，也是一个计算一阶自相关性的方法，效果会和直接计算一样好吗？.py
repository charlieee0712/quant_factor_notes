"""DW统计量，也是一个计算一阶自相关性的方法，效果会和直接计算一样好吗？

自动生成自 ../sources/zhengzhaolei/articles/2025-05-15_DW统计量，也是一个计算一阶自相关性的方法，效果会和直接计算一样好吗？.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/zhengzhaolei/articles/images/2025-05-15_DW统计量，也是一个计算一阶自相关性的方法，效果会和直接计算一样好吗？/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: zhengzhaolei
# 公众号  : 量化拯救散户
# 标题    : DW统计量，也是一个计算一阶自相关性的方法，效果会和直接计算一样好吗？
# 日期    : 2025-05-15
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247488789&idx=1&sn=a57058e931294cab4d552a66124987a8
# 本地原文: ../sources/zhengzhaolei/articles/2025-05-15_DW统计量，也是一个计算一阶自相关性的方法，效果会和直接计算一样好吗？.md
# 本地图片: ../sources/zhengzhaolei/articles/images/2025-05-15_DW统计量，也是一个计算一阶自相关性的方法，效果会和直接计算一样好吗？/  (共 23 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:29

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# 上一篇文章中，笔者复现了一阶自相关性因子。其中，成交量占比和成交额占比的一阶自相关性因子的表现非常好，有三年IC的绝对值超过了0.1，同时分层回测的单调性也还算不错。
#
# 那么，本文将介绍一种新的一阶自相关性的计算方法，叫做DW统计量，其公式如下：
#
# 我们直接计算收益率与成交量占比时序数据的 D-W 统计量，其统计量的绝对大小可以判定时序数据是否存在自相关性，以及相关性的正负性。这段话是研报中的原文，笔者也就是按照这个思路来计算DW统计量的。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 收益分析
# diff
#
# 这一次，成交额占比的DW统计量和成交量占比的DW统计量之差这个因子，用均值合成月度因子的表现好于标准差。
#
# 在IC方面，均值的绝对值更大。
#
# 在分层回测方面，单调性也更好。如果用标准差的话，那么因子值最大的一组和因子值最小的一组，年化收益率小于其他三组。
#
# ## 收益分析
# 总结：看来DW统计量和自相关性的差异还是挺大的，至少从笔者的复现情况来看是这样的。值得一提的是，在研报中，用DW统计量也会使IC绝对值下降，但是好像也没有笔者复现的结果这么夸张。
#

# ============================================================
# 本地图片清单（共 23 张）
# ============================================================
# 001.png  <-  (文章开头)
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  回归分析
# 009.png  <-  回归分析
# 010.png  <-  换手率分析
# 011.png  <-  换手率分析
# 012.png  <-  收益分析
# 013.png  <-  IC分析
# 014.png  <-  IC分析
# 015.png  <-  IC分析
# 016.png  <-  IC分析
# 017.png  <-  IC分析
# 018.png  <-  IC分析
# 019.png  <-  回归分析
# 020.png  <-  回归分析
# 021.png  <-  换手率分析
# 022.png  <-  换手率分析
# 023.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    # 加载当日分钟数据
    file_name = self.files[idx]
    full_path = os.path.join(self.file_pth, file_name)
    data = BaseDataLoader.load_data(full_path, fields=['volume', 'close', 'open', 'turnover'])
    date_str = file_name.split('.')[0]

    vol = data.to_dataframe('volume')
    vol = vol.div(vol.sum(axis=0), axis=1)
    money = data.to_dataframe('turnover')
    money = money.div(money.sum(axis=0), axis=1)
    ret = (data.to_dataframe('close') / data.to_dataframe('open')) - 1

    rtn_dw = np.square(ret.diff()).sum() / np.square(ret.sum())
    vol_dw = np.square(vol.diff()).sum() / np.square(vol.sum())
    money_dw = np.square(money.diff()).sum() / np.square(money.sum())

    res = pd.concat([rtn_dw, vol_dw, money_dw, money_dw - vol_dw], axis=1)
    res.columns = ['rtn_dw', 'vol_dw', 'money_dw', 'diff']
    res['datetime'] = pd.to_datetime(date_str) + timedelta(hours=15)
    res.index.name = 'code'
    return res.reset_index()

# (作者注) 这段代码和上一篇文章中的代码大部分是一样的，核心的内容在14-15行。其实，也就是按照DW统计量的公式计算DW统计量。
#
# (作者注) rtn\_dw
#
# (作者注) 这个因子如果用标准差合成的话那简直是辣眼睛，因为IC绝对值最高的一年也就勉强超过了0.01。同时，2018年，2021年和2024年这三年的IC为负，其他年份的IC为正。
#
# (作者注) 本以为换成均值会好一点点，结果一看两种方式半斤八两，都不值得展示了。
#
# (作者注) vol\_dw
#
# (作者注) 这个因子用均值合成的效果稍微好一点点。
#
# (作者注) 如果用标准差合成的话，那又是一个无用的因子，或者说是辣眼睛的因子。虽然，这个因子用标准差合成IC绝对值最大超过了0.03，但是很不稳定，2019年和2024年的因子IC为负，其他年份的因子IC为正。

