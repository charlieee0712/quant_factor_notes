"""学术论文中的五大波动率因子，一篇文章给你讲完！

自动生成自 ../sources/chenshengrui/articles/2025-11-15_学术论文中的五大波动率因子，一篇文章给你讲完！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/chenshengrui/articles/images/2025-11-15_学术论文中的五大波动率因子，一篇文章给你讲完！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: chenshengrui
# 公众号  : 量化拯救散户
# 标题    : 学术论文中的五大波动率因子，一篇文章给你讲完！
# 日期    : 2025-11-15
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247491633&idx=1&sn=475c413d080e670285d2ae920595f7be
# 本地原文: ../sources/chenshengrui/articles/2025-11-15_学术论文中的五大波动率因子，一篇文章给你讲完！.md
# 本地图片: ../sources/chenshengrui/articles/images/2025-11-15_学术论文中的五大波动率因子，一篇文章给你讲完！/  (共 20 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:28

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **波动率因子**
#
# 本文，笔者将介绍一下学术论文中提到的五种不同的波动率计算方式。
#
# 这五种不同的波动率计算方法考虑了不同的价格信息，也来自不同的论文。但是，笔者没有看过这些论文，而是在中信建投陈升锐老师2025年10月18日发布的研报《高频流动性与波动率因子再构建》中看到的。
#
# 本来，笔者是计划这篇文章复现一下陈老师对凸显理论因子的改进思想的。结果，昨天晚上有一位大佬发给了笔者这篇研报，所以笔者决定，提前复现一下这篇研报中的内容。
#
# **计算公式和代码**
#
# 这五个不同的波动率因子，每个都考虑了不同的价格。
#
# 同时，研报中还给出了这五个波动率因子的增强版本，就是用计算出来的波动率然后除上成交额。
#
# **1**
#
# **计算公式**
#
# 第一个是用Parkinson方法计算的，公式如下：
#
# 这种计算方式的优点是用极差估计当天波动率效率高，缺点是无法处理跳空会系统性低估风险。
#
# 第二种方法，Garman-Klass方法。
#
# 它的优点是使用了收盘价数据充分利用了信息，遗憾的是它依旧无法处理跳空。
#
# 第三种方法，Rogers-Satchell方法。
#
# 它仍旧没能处理跳空，但是它在趋势强劲的时候可以有效估计。
#
# 第四种方法，Yang-Zhang方法。
#
# 这个方法终于将跳空考虑了进去，同时它还有估计误差小，仅用较少数据就能达到较好效果的优点。但是，开盘跳空时，它效率低下。
#
# 需要注意的是，这个波动率的计算中有个k值，k值的计算公式里面有个alpha参数，笔者通过deepseek了解到这个alpha可以设置为0.34。
#
# 第五种方法，Cho-Free方法。
#
# 它的优点是具有理想的渐进性，至于缺点，研报中没有提到。
#
# 这里，笔者有个困惑，那就是delta到底等于什么，最后一个数字看起来像8，又感觉不是。但是，笔者最后还是用8来计算的。
#
# **2**
#
# **代码**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 从相关性来看，只有Yang-Zhang和Cho-Free这两种方法计算的波动率和传统波动率因子的相关性低于0.7。
#
# 同时，增强因子之间的相关性都超过了0.7，因为主导它们的已经变成了成交额。如果直接计算成交额的倒数的话，和它们的相关性也不会太低。
#
# 所以，这里的因子评价就只展现波动率的结果了，而且只展现Cho-Free方法得到的波动率因子。
#
# 因为，前三种计算方法得到的波动率因子和传统波动率相关性高，而且分层回测的表现也差不多。至于Yang-Zhang和Cho-Free中选择后者，是因为它的分层回测看起来好那么一点点。
#
# 值得一提的是，无论是Yang-Zhang还是Cho-Free，它们的IC低于前三种计算方式得到的因子。
#
# ## IC分析
# 关于IC，笔者想说一点。
#
# 波动率因子的IC为负，而波动率增强因子和波动率因子的相关性为负，这意味着波动率增强因子的IC为正。可以简单推出成交额的倒数大的标的，收益率高，这不就是小盘股甚至微盘股吗。
#
# ## 收益分析
# 分层回测前半段的表现可以说是很差了，在2022年之前，这五组之间可以说是毫无区分度。
#
# 到了2022年之后，因子值最小的两组收益率开始超过了因子值大的三组，这和IC也能对应上（后面四年的IC绝对值大于前面三年）。
#
# 补一个，增强因子的分层回测结果吧，说实话看起来确实比不增强的好。但是，这个效果是成交额带来的。
#
# 最后，感谢各位大佬的支持，也希望大家能够继续支持，帮忙点点赞、分享分享，收藏收藏，甚至可以推荐推荐。当然，没有关注的大佬可以点点关注。
#

# ============================================================
# 本地图片清单（共 20 张）
# ============================================================
# 001.png  <-  计算公式
# 002.png  <-  计算公式
# 003.png  <-  计算公式
# 004.png  <-  计算公式
# 005.png  <-  计算公式
# 006.png  <-  代码
# 007.png  <-  因子评价
# 008.png  <-  IC分析
# 009.png  <-  IC分析
# 010.png  <-  IC分析
# 011.png  <-  IC分析
# 012.png  <-  IC分析
# 013.png  <-  IC分析
# 014.png  <-  回归分析
# 015.png  <-  回归分析
# 016.png  <-  换手率分析
# 017.png  <-  换手率分析
# 018.png  <-  收益分析
# 019.png  <-  收益分析
# 006.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def __call__(self):
    data = BaseDataLoader.load_data('../../data/stock_bar_1day.parquet',
                                         fields=['high', 'low', 'close', 'open', 'turnover']).to_dataframes()
    sigma_normal = data['close'].pct_change().rolling(21).std()
    hl_ratio = data['high'] / data['low']
    co_ratio = data['close'] / data['open']
    hc_ratio = data['high'] / data['close']
    ho_ratio = data['high'] / data['open']
    lc_ratio = data['low'] / data['close']
    lo_ratio = data['low'] / data['open']
    sigma_p = np.sqrt((np.square(np.log(hl_ratio))).rolling(21).sum() / (84*np.log(2)))
    sigma_gk = np.sqrt((0.5 * np.square(np.log(hl_ratio)) -
                          (2 * np.log(2) - 1) * np.square(np.log(co_ratio))).rolling(21).mean())
    sigma_rs = (np.log(hc_ratio) * np.log(ho_ratio) +
                        np.log(lc_ratio) * np.log(lo_ratio)).rolling(21).mean()
    sigma_o = (np.log(data['open'] / data['close'].shift(1))).rolling(21).mean()
    sigma_c = (np.log(data['close'] / data['open'].shift(1))).rolling(21).mean()
    k = 0.34 / (1.34 + 22 / 20)
    sigma_yz = np.sqrt(sigma_o + k * sigma_rs + (1 - k) * sigma_c)
    sigma_rs = np.sqrt(sigma_rs)
    delta = np.log(9/8)
    delta_close = (data['close'].diff()).rolling(21).mean()
    mu = delta_close / (2 * delta)
    tao = delta_close / 21
    sigma_cf = np.sqrt(2 * mu * delta / (np.log(delta + mu * tao) - np.log(tao - mu * tao)))
    dfs = {'sigma_normal': sigma_normal, 'sigma_p': sigma_p, 'sigma_gk': sigma_gk,
           'sigma_rs': sigma_rs, 'sigma_yz': sigma_yz, 'sigma_cf': sigma_cf}
    keys = list(dfs.keys())
    for key in keys:
        new_key = key + '_i'
        dfs[new_key] = dfs[key] / data['turnover']
    df = dfs_to_df(dfs)
    df.to_parquet('sigma.parquet')

# (作者注) 这个因子的代码并不是很难，都是基于日频数据计算的，只是公式有点复杂。
#
# (作者注) 第2-3行读取数据。
#
# (作者注) 第4行，计算的是传统的波动率，就是过去21个交易日收益率的标准差，用来和这五种方式计算的结果进行对比，主要就是看看相关性。
#
# (作者注) 第5-10行，计算各种各样的价格比，用来计算波动率。
#
# (作者注) 第11行，计算的是Parkinson方法得到的波动率。
#
# (作者注) 第12行，计算的是Garman-Klass方法得到的波动率。
#
# (作者注) 第13行，计算的是Rogers-Satchell方法得到的波动率的平方，因为计算Yang-Zhang的时候要用到，所以等计算完Yang-Zhang波动率之后再开方。
#
# (作者注) 第16-19行，计算的是Yang-Zhang方法得到的波动率。
#
# (作者注) 第21-25行，计算的是Cho-Free方法得到的波动率。

