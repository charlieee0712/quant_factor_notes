"""峰岭成交比因子，IC和分层回测都表现不错！

自动生成自 ../sources/weijianrong/articles/2025-11-05_峰岭成交比因子，IC和分层回测都表现不错！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/weijianrong/articles/images/2025-11-05_峰岭成交比因子，IC和分层回测都表现不错！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: weijianrong
# 公众号  : 量化拯救散户
# 标题    : 峰岭成交比因子，IC和分层回测都表现不错！
# 日期    : 2025-11-05
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247491403&idx=1&sn=5c2277221d5b56a76afe99902eec6149
# 本地原文: ../sources/weijianrong/articles/2025-11-05_峰岭成交比因子，IC和分层回测都表现不错！.md
# 本地图片: ../sources/weijianrong/articles/images/2025-11-05_峰岭成交比因子，IC和分层回测都表现不错！/  (共 13 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:28

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **峰岭成交比**
#
# 在今年八月的时候，笔者复现了一个热乎的因子，它来自开源证券魏建榕老师的研报。
#
# 在这篇研报中出现了几十个因子，笔者前面分了四篇文章来介绍其中的部分因子。由于介绍到中间的时候，发现因子效果算不上优秀，所以就暂停了一段时间。
#
# 这次，重新拾起，是因为笔者觉得改变一下顺序，先复现研报种IC高的因子。
#
# 之前和这篇研报有关的文章如下，供大家参考。
#
# [高频成交量的峰、岭、谷信息（一）：2025年7月20日应该还热乎的研报，IC和分层回测都还不错](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247489605&idx=1&sn=e301d0cbc940fa40ed8d0731a5796eb5&scene=21#wechat_redirect)
#
# [高频成交量的峰、岭、谷信息（二）：分钟收益率因子，整体不如分钟数](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247489653&idx=1&sn=1e4c57fdfe2d951aee40c7b911e0d6e4&scene=21#wechat_redirect)
#
# [高频成交量的峰、岭、谷信息（三）：相对加权价格因子，这次的结果和研报产生了差异！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247489735&idx=1&sn=78679d683daf585cef394208d8a1f708&scene=21#wechat_redirect)
#
# [高频成交量的峰、岭、谷信息（四）：加权价格分位点因子，分层回测表现不错！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247489785&idx=1&sn=de66ba27a23e7657cafada9fc5d16c15&scene=21#wechat_redirect)
#
# [高频成交量的峰、岭、谷信息勘误](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247489922&idx=2&sn=ea8a2f11de01bace0e00adbb9fb9fc20&scene=21#wechat_redirect)
#
# **计算方法和代码**
#
# 这个因子的计算核心是将成交量划分为喷发时刻和不喷发时刻，然后将孤立的喷发时刻作为量峰时刻，连续的喷发时刻作为量岭时刻，不喷发时刻则是量谷时刻。
#
# 具体的划分方式在第一篇文章中有介绍，这里就不赘述了，仅介绍一下何谓峰岭成交额比。
#
# **1**
#
# **计算方法**
#
# 峰岭成交量比，顾名思义就是量峰时刻的成交量和量岭时刻成交量的比值。
#
# 不过，研报中有这样一句话。计算过去20日量峰总成交额与量岭总成交额的比值。
#
# 也就是说，这个因子需要先计算每日的量岭成交额，然后再计算各自过去21个交易日的和，最后计算两者的比值。
#
# **2**
#
# **代码**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 在因子评价的时候，笔者不仅尝试了峰岭成交比，还试了峰谷成交比、岭谷成交比，效果都不如峰岭成交比。
#
# 同时，笔者还尝试了每日计算一个峰岭成交比，然后对过去21个交易日的峰岭成交比计算均值或者标准差的方式，也不如研报中提到的这种方式，所以这里展示的峰岭成交比因子是严格按照计算方法中的内容计算得到的。
#
# ## IC分析
# 从IC上的表现来看，这个因子还是不错的，有两年的IC超过了0.1，除了2024年外其他年份的IC都超过了0.08。
#
# ## 收益分析
# 分层回测的话就有一点点遗憾了，group\_2的收益率超过了group\_3。不过，多空收益还是不错的。
#
# 最后，希望各位大佬多多支持，点赞、分享、收藏和喜欢多点一点，谢谢大家了。
#

# ============================================================
# 本地图片清单（共 13 张）
# ============================================================
# 001.png  <-  代码
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  回归分析
# 009.png  <-  回归分析
# 010.png  <-  换手率分析
# 011.png  <-  换手率分析
# 012.png  <-  收益分析
# 001.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    if idx < 20:
        return pd.DataFrame({})
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]
    vol = []
    for i in range(idx - 20, idx + 1):
        file_name = self.files[i]
        full_path = os.path.join(self.file_pth, file_name)
        data = BaseDataLoader.load_data(full_path, fields=['volume', 'turnover'])
        vol.append(data.to_dataframe('volume'))
    flag = self.cal_flag(vol)
    money = data.to_dataframe('turnover').reindex(columns=flag.columns.tolist())
    money_valley = (cur_vol * np.where(flag == 0, 1, np.nan)).sum()
    money_peak = (cur_vol * np.where(flag == 1, 1, np.nan)).sum()
    money_ridge = (cur_vol * np.where(flag == 2, 1, np.nan)).sum()
    res = pd.concat([vol_valley, vol_peak, vol_ridge], axis=1)
    res.columns = ['valley', 'peak', 'ridge']
    res['datetime'] = pd.to_datetime(date_str) + timedelta(hours=15)
    return res

# (作者注) 前11行代码，读取数据。
#
# (作者注) 第12行代码，调用cal\_flag方法得到峰、岭和谷对应的时刻。具体的代码在之前的文章有，有需要的读者可以自取一下。
#
# (作者注) 第13行，读取最新一个交易日的成交额数据，然后和flag这个dataframe的列对齐。
#
# (作者注) 第14-15行，分别计算谷、峰和岭时刻的成交额之和。

