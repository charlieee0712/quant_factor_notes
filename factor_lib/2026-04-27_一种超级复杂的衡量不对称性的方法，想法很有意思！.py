"""一种超级复杂的衡量不对称性的方法，想法很有意思！

自动生成自 ../sources/general/articles/2026-04-27_一种超级复杂的衡量不对称性的方法，想法很有意思！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2026-04-27_一种超级复杂的衡量不对称性的方法，想法很有意思！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 一种超级复杂的衡量不对称性的方法，想法很有意思！
# 日期    : 2026-04-27
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247496418&idx=1&sn=d94cb6aa5885776e5c6ca21b22c336f1
# 本地原文: ../sources/general/articles/2026-04-27_一种超级复杂的衡量不对称性的方法，想法很有意思！.md
# 本地图片: ../sources/general/articles/images/2026-04-27_一种超级复杂的衡量不对称性的方法，想法很有意思！/  (共 15 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **不对称性**
#
# 通常来说，常见的衡量不对称性的方法主要是偏度。在大A这个市场中，偏度已经被广泛使用了，收益率的偏度、成交额的偏度，这些都是很常见的因子了。
#
# 最近，笔者找到了一篇关于不对称性的研报，里面的内容可以说是给了我们不少新的工具。这篇研报是东方证券朱剑涛老师在2021年12月25日发布的《因子选股系列研究之八十：收益率的非对称分布与尾部蕴含的Alpha》。
#
# 之前，笔者已经复现了这篇研报中的一个因子，具体可以参考[收益率的不对称性，2007年老外的研究和MMD结合之后，IC居然还挺高！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247496255&idx=1&sn=5fe8e8f50869853fc4ca928489b48580&scene=21#wechat_redirect)。
#
# 不得不说，这篇文章似乎不合各位大佬的胃口，因为点赞推荐和分享都不多。可能是因为太简单了，所以本文选择了一个复杂的非对称性的度量，希望能合各位大佬的胃口。
#
# 如果您对本文分享的内容感兴趣的话，希望各位大佬能够动动金手，帮忙点赞关注推荐和分享都点一点。
#
# 既然都是衡量收益率的不对称性的，那么其背后的逻辑都是一样的。因此，这里就不聊逻辑了。
#
# 那么，为什么要用一个复杂的东西来衡量不对称性呢？因为，咱们国内的学者发现，当一个不对称的分布无限接近对称的时候，偏度很难识别里面的非对称性了，但是这个方法可以。
#
# **计算公式和代码**
#
# 从计算步骤上来看，这个因子的计算可以说是一点都不复杂的，甚至可以说是只有一个公式。
#
# 但是，要计算这个公式中的变量确是很复杂的，研报中也是一带而过，笔者只能让AI帮忙写代码来实现计算了。
#
# **1**
#
# **计算公式**
#
# 没错，整个计算的难点就是如何估计收益率的概率密度函数和分布函数，而且估计的方法会对因子的结果造成一定的影响。
#
# 这也触及到笔者的知识盲区了，只能通过AI来实现这个因子的计算了。
#
# 如果有大佬对这一方面很熟悉的话，可以在评论区狠狠教育笔者一番，笔者一定虚心学习。
#
# **2**
#
# **代码**
#
# 下面的代码都是AI生成的，笔者只是当了一次搬运工。
#
# 由于AI的代码都有注释的，笔者就只对输入数据的格式进行一个说明了。
#
# 这里的输入数据是一个series，具体为一个标的一天239分钟的收益率。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 在笔者测试的时候发现，这个因子用均值进行低频化处理在各个方面的表现都是优于标准差的。因此，这里将只展示用均值低频化的因子评价结果。
#
# ## IC分析
# 说实话，这个IC的表现挺一般的，甚至还不如[收益率的不对称性，2007年老外的研究和MMD结合之后，IC居然还挺高！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247496255&idx=1&sn=5fe8e8f50869853fc4ca928489b48580&scene=21#wechat_redirect)这篇文章中提到的因子。
#
# 原因至少有两个方面。
#
# 第一，AI没有理解这个因子的精髓，其概率密度函数和分布函数的估计存在缺陷。
#
# 第二，那就是MMD太强了。
#
# ## 收益分析
# 这个分层回测的表现也并不是很好。
#
# 在2022年之前，五组分层回测的净值曲线之间的差距并不大，2022年之后，因子值最大的两组开始落后，但是因子值最小的三组之间依旧无法区分开来。
#

# ============================================================
# 本地图片清单（共 15 张）
# ============================================================
# 001.png  <-  计算公式
# 002.png  <-  代码
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  回归分析
# 010.png  <-  回归分析
# 011.png  <-  换手率分析
# 012.png  <-  换手率分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 015.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def __cal_factors__(self, data):
    data = data.dropna()
    if data.std() == 0:
        return 0
    if len(data) < 10:
        return np.nan  # 核密度估计至少需要10个样本
    # 2. 核密度估计（拟合 PDF）
    kde = gaussian_kde(data, bw_method='scott')
    # 3. 构建 x 轴网格（覆盖收益率主要分布区间）
    mu, sigma = data.mean(), data.std()
    x_min = mu - 3 * sigma
    x_max = mu + 3 * sigma
    x_grid = np.linspace(x_min, x_max, 1000)
    # 4. 计算网格上的 PDF 与 CDF
    pdf_grid = kde.evaluate(x_grid)  # PDF 值
    # 梯形法累积积分 → CDF
    dx = x_grid[1] - x_grid[0]
    cdf_grid = cumulative_trapezoid(pdf_grid, dx=dx, initial=0)
    cdf_grid = np.clip(cdf_grid, 0.0, 1.0)  # 数值截断
    # 5. 将原始收益率点映射到 CDF 曲线上
    pdf_xi = kde.evaluate(data)  # 每个 xi 处的 PDF 值
    cdf_xi = np.interp(data, x_grid, cdf_grid)  # 每个 xi 处的 CDF 值
    # 6. 计算相关系数 ρ[ PDF(xi), CDF(xi) ]
    r, _ = pearsonr(pdf_xi, cdf_xi)
    # 7. 最终因子 Asym = -ρ
    asym = -r
    return asym

