"""有心栽花花不开，无心插柳柳成荫！邪修的副产品，分层回测竟然差一点就“完美”了！

自动生成自 ../sources/general/articles/2026-05-13_有心栽花花不开，无心插柳柳成荫！邪修的副产品，分层回测竟然差一点就“完美”了！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2026-05-13_有心栽花花不开，无心插柳柳成荫！邪修的副产品，分层回测竟然差一点就“完美”了！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 有心栽花花不开，无心插柳柳成荫！邪修的副产品，分层回测竟然差一点就“完美”了！
# 日期    : 2026-05-13
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247496687&idx=1&sn=8c7f6dab94daff1b905632b53fa3fab5
# 本地原文: ../sources/general/articles/2026-05-13_有心栽花花不开，无心插柳柳成荫！邪修的副产品，分层回测竟然差一点就“完美”了！.md
# 本地图片: ../sources/general/articles/images/2026-05-13_有心栽花花不开，无心插柳柳成荫！邪修的副产品，分层回测竟然差一点就“完美”了！/  (共 17 张)
# 段落识别: PARTIAL  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **成交量的可预测性**
#
# 有一位大佬建议笔者改名“邪修量化”。
#
# 作为一个听劝的人，这次没有采纳这位大佬的建议。
#
# 不过，笔者看了下发文记录，五月份过去了三分之一了，居然一篇“邪修”都没有。
#
# 于是，就开造！

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# 这次，笔者想用过去21个交易日每分钟的成交量中位数作为解释变量，当天每分钟的成交量作为被解释变量，进行时序回归，然后回归的系数就是成交量放大倍数。
#
# 在进行因子计算的时候，笔者顺便算了一下R方。
#
# 结果，在因子评价的时候发现用R方选股的效果更好，所以就有了这个叫做成交量的可预测性的因子。
#
# 第一步，如前面所述，过去21个交易日每分钟的成交量中位数作为解释变量，当天每分钟的成交量作为被解释变量，进行时序回归。
#
# 第二步，计算回归的R方。
#

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 这个因子的发现过程，可以用一句古诗来形容。
#
# 有心栽花花不开，无心插柳柳成荫。
#
# 本来是想通过斜率来选股的，没想到R方的表现大大地抢了风头。
#
# 用均值进行低频化处理之后的因子在IC上的表现好于用标准差低频化的，至于分层回测，两者都是离完美差了一点点。所以，这里将以均值低频化为主展现因子评价结果。
#
# ## IC分析
# 这个因子前几年的IC表现还行，从2022年开始逐年下降。看起来，有失效的趋势。
#
# 至于斜率，其IC累积仅勉强超过-3，远低于R方。
#
# ## 收益分析
# 首先来看看用均值低频化后的分层回测结果，可以说还行。
#
# 如果只看最后的净值的话，那就是差一点点完美，因为橙色的线的净值高于蓝色的线。
#
# 至于用标准差低频化的因子，其因子值最大的两组从一开始就落后于其他组了，而因子值最低的一组最终的净值也是最高的。
#
# 遗憾的是，绿线的净值超过了黄线，单调性还是不完美。
#
# ## 总结
# 总的来说，这次邪修不算失败。
#
# 那么，这个因子能不能用呢？得看看它样本外的表现了（笔者展示的都是样本内的结果）。
#
# 如果把2023年和2024年看成样本外的话，这个因子失效的可能性还是挺大的。
#
# 好了，这次的邪修到这里就结束了，希望各位大佬能够喜欢，也希望各位大佬能多多点赞推荐分享和关注支持一下笔者！
#

# ============================================================
# 本地图片清单（共 17 张）
# ============================================================
# 001.png  <-  代码
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  回归分析
# 009.png  <-  回归分析
# 010.png  <-  换手率分析
# 011.png  <-  换手率分析
# 012.png  <-  收益分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 015.png  <-  收益分析
# 016.png  <-  收益分析
# 016.png  <-  总结

# ============================================================
# 作者代码（按原文出现顺序，共 2 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    if idx < 20:
        return pd.DataFrame({})
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]
    vol = []
    for i in range(idx - 20, idx + 1):
        file_name = self.files[i]
        full_path = os.path.join(self.file_pth, file_name)
        data = BaseDataLoader.load_data(full_path, fields=['volume'])
        vol.append(data.to_dataframe('volume'))
    x = self.cal_med(vol)
    y = vol[-1]
    x = x.reindex(columns=y.columns)
    res = []
    for i in range(x.shape[1]):
        res.append(self.cal_factors(x.iloc[:, i].values, y.iloc[:, i].values))
    res = pd.DataFrame(res, index=x.columns, columns=['slope', 'r2'])
    res['datetime'] = pd.to_datetime(date_str) + timedelta(hours=15)
    return res

# (作者注) 第7-11行，读取t-20到t日一共21个交易日的成交量数据。
#
# (作者注) 第12行，计算成交量的中位数。
#
# (作者注) 第13行，获取当日的成交量。
#
# (作者注) 第14行，将x和y的数据维度对齐。
#
# (作者注) 第15-17行，进行时序回归，并返回斜率和R方。

# --- 代码块 2 ---
def cal_med(self, data):
    data = pd.concat(data)
    data['minute'] = data.index.hour * 60 + data.index.minute
    data = data.groupby('minute', as_index=False, group_keys=False).apply(self._cal_med_)
    return data

@staticmethod
def _cal_med_(group):
    group = group.drop(columns=['minute'])
    group = group.median()
    return group.to_frame().T

@staticmethod
def cal_factors(x, y):
    flag = ~np.isnan(x) & ~np.isnan(y)
    x, y = x[flag].reshape(-1, 1), y[flag].reshape(-1, 1)
    lr = LinearRegression()
    lr.fit(x, y)
    pred = lr.predict(x)
    r2 = r2_score(y, pred)
    return [lr.coef_[0][0], r2]

# (作者注) 这三个方法都挺简单的，前两个用来计算同分钟数据的中位数，第三个则是调用sklearn的包来进行线性回归，并返回斜率和R方。

