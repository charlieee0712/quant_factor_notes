"""邪修！同价波动率中的alpha因子，一下整出来7个因子，而且IC都还不错！

自动生成自 ../sources/general/articles/2026-02-03_邪修！同价波动率中的alpha因子，一下整出来7个因子，而且IC都还不错！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2026-02-03_邪修！同价波动率中的alpha因子，一下整出来7个因子，而且IC都还不错！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 邪修！同价波动率中的alpha因子，一下整出来7个因子，而且IC都还不错！
# 日期    : 2026-02-03
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247494618&idx=1&sn=e79aa47340f69924ff9f6fa6ed1f2b7b
# 本地原文: ../sources/general/articles/2026-02-03_邪修！同价波动率中的alpha因子，一下整出来7个因子，而且IC都还不错！.md
# 本地图片: ../sources/general/articles/images/2026-02-03_邪修！同价波动率中的alpha因子，一下整出来7个因子，而且IC都还不错！/  (共 19 张)
# 段落识别: PARTIAL  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **同价波动率**
#
# 同价波动率这个因子是笔者在尝试复现国盛证券《Alpha掘金系列之六：弹性与投资者耐心-基于高频订单簿的斜率凸性因子》时邪修出来的。
#
# 众所周知，笔者是没有逐笔数据，也没有订单簿数据的。因此，对于此类因子只能理解其思想，然后用分钟级数据尝试复现一下。
#
# 这个因子，笔者想通过同价成交量的斜率来尝试一下，但是效果不是很好。不过，在这次尝试的过程中，笔者有了一个新的思路，同价波动率，然后根据同一价格不同分钟的收益率来计算波动率，并以此来构建因子。
#
# 这个新的邪修出来的因子，其主要思想参考的是兴业证券郑兆磊老师的同价成交量分布中的alpha因子，关于这个因子笔者在[同价成交量分布中alpha因子，IC和分层回测都还不错](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247488301&idx=1&sn=e9338199d3b01c6c34cdc5b5b38b87f3&scene=21#wechat_redirect)这篇文章中复现过。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# 这个因子的计算思想并不是很复杂，但是实现起来也并不是那么的简单，所以一起来看一看吧。
#
# 第一步，计算分钟收益率，即收盘价/开盘价-1。
#
# 第二步，计算同一收盘价所有分钟的收益率的标准差，即同价波动率。
#
# 第三步，取波动率最高和最低的两个价格，分别记为high和low。
#
# 第四步，计算high/最高收盘价，high/最低收盘价，high/收盘价，low/最高收盘价，low/最低收盘价，low/收盘价和high/low，一共7个因子。
#

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 这7个因子之间的相关性有高有低，高的接近0.85，低的接近0，差异还是挺大的。
#
# 由于，之前的同价成交量分布alpha因子，笔者发现用过去21个交易日的均值除以标准差合成的因子效果较好，因此这个因子也是用同样的方式来合成的。
#
# 同时，low2max这个因子的表现是最好的，所以下面展示的将是这个因子的因子评价结果。
#
# ## IC分析
# 这个因子的IC看起来还是不错的，有5年超过了0.08。
#
# ## 收益分析
# 这个是用均值/标准差合成因子的分层回测结果，其表现是最好的。
#
# 接下来看看单独用均值合成的因子的分层回测结果。
#
# 最后看看单独用标准差合成的因子的分层回测结果。
#
# 显然，标准差合成的因子和均值合成的因子，方向是相反的。
#
# 总的来说，这个因子的IC和分层回测都还算能看。但是，见过了好的因子之后，这个因子的结果肯定是不能让各位大佬满意的。笔者在复现完这个因子之后，想到了一个改进的方法，将波动率换成振幅。因为波动率需要至少两条数据才能计算，这样的话有些收盘价的波动率会变成nan。
#
# 好了，废话也说完了，这篇文章也正式结束了，先给大家预告一下，这个周末不会有更新了，因为笔者要出去玩上几天了。旅游回来继续更新，所以希望大家能继续支持笔者，帮忙点点推荐关注分享和赞。
#

# ============================================================
# 本地图片清单（共 19 张）
# ============================================================
# 001.png  <-  代码
# 002.png  <-  因子评价
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  回归分析
# 010.png  <-  回归分析
# 011.png  <-  换手率分析
# 012.png  <-  换手率分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 015.png  <-  收益分析
# 016.png  <-  收益分析
# 017.png  <-  收益分析
# 018.png  <-  收益分析
# 001.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 2 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]
    # 加载当日分钟数据
    full_path = os.path.join(self.file_pth, file_name)
    data = BaseDataLoader.load_data(full_path, fields=['close', 'open'])
    res = []
    for i in range(len(data.codes)):
        tmp_data = pd.DataFrame(data=data.data[:, :, i], columns=['close', 'open'])
        res.append(self.cal_factors(tmp_data))
    res = pd.DataFrame(data=res, index=data.codes.tolist(), columns=['high2min', 'high2max', 'high_ratio',
                                                                     'low2min', 'low2max', 'low_ratio', 'high2low'])
    res['datetime'] = pd.to_datetime(date_str) + timedelta(hours=15)
    res.index.name = 'code'
    return res.reset_index()

# (作者注) 第一段代码主要就是读取数据。
#
# (作者注) 第8-11行，按照标的循环然后调用cal\_factors计算因子。

# --- 代码块 2 ---
@staticmethod
def cal_factors(data):
    close = data.iloc[-1, 0]
    high = np.nanmax(data.iloc[:, 0])
    low = np.nanmin(data.iloc[:, 0])
    data['open'] = data['close'] / data['open'] - 1
    data = data.groupby('close').std()
    data.sort_index(inplace=True)
    data.reset_index(inplace=True)
    max_idx = np.argmax(data['open'])
    max_price = data.iloc[max_idx]['close']
    min_idx = np.argmin(data['open'])
    min_price = data.iloc[min_idx]['close']
    return [max_price / low, max_price / high, max_price / close,
            min_price / low, min_price / high, min_price / close, max_price / min_price]

# (作者注) 第3-5行，分别获取收盘价、最高收盘价和最低收盘价。
#
# (作者注) 第6行，计算分钟收益率。
#
# (作者注) 第7行，按照收盘价进行聚合，并计算波动率。
#
# (作者注) 第10-11行，获取波动率最大的收盘价。
#
# (作者注) 第12-13行，获取波动率最小的收盘价。
#
# (作者注) 第14-15行，返回因子。

