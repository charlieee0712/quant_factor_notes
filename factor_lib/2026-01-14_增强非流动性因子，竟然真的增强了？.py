"""增强非流动性因子，竟然真的增强了？

自动生成自 ../sources/chenshengrui/articles/2026-01-14_增强非流动性因子，竟然真的增强了？.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/chenshengrui/articles/images/2026-01-14_增强非流动性因子，竟然真的增强了？/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: chenshengrui
# 公众号  : 量化拯救散户
# 标题    : 增强非流动性因子，竟然真的增强了？
# 日期    : 2026-01-14
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247494084&idx=1&sn=0cc067d4512e9cfd147029674c682c37
# 本地原文: ../sources/chenshengrui/articles/2026-01-14_增强非流动性因子，竟然真的增强了？.md
# 本地图片: ../sources/chenshengrui/articles/images/2026-01-14_增强非流动性因子，竟然真的增强了？/  (共 17 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:28

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **增强非流动性**
#
# 似乎自从Amihud提出非流动性因子之后，无论是学术界还是业界都对这个因子充满了兴趣，感觉大家都希望利用非流动性溢价来套利。
#
# 如果没记错的话，本文应该是笔者从2025年开始写的第五篇关于非流动性的文章了。如果只看分层回测的话最好的应该是这篇[基于K线最短路径构造的非流动性因子，分层回测的效果有点惊艳！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247487661&idx=1&sn=c25570f33538ba91e76745ca7f904174&scene=21#wechat_redirect)文章介绍的因子。（需要注意的是，那时候的因子有两个错误，计算的时候没考虑复权因子，分层回测也没考虑。）
#
# 最近复现的这两篇[用换手率来计算非流动性，来自绿皮书作者的另一本书，在大A会有怎样的表现呢？](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247492599&idx=1&sn=03048ff9440bd39dd689ca9dff8b945f&scene=21#wechat_redirect)和[非流动性因子改得这么复杂，效果真的会好吗？](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247493633&idx=1&sn=f31ada053ccebe1002826a8dc1945180&scene=21#wechat_redirect)（和本文来自同一篇研报，中信建投的陈升锐老师在2022年10月发布的高频因子选股系列五：流动性因子系统解读与再增强》），在计算因子的时候没有考虑复权因子。
#
# 所以，在写这篇文章之前，笔者重新去计算了一下这两篇文章中的因子，结论并没有什么变化。
#
# **计算公式和代码**
#
# 、
#
# 、
#
# 基于日频收益率计算的因子大都有这个问题，在[终于找到分层回测每组都是亏损的原因了！原来是又又又写错代码了，差点所有的因子都要重来！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247490693&idx=1&sn=f74afd5b641f4ac523548f7585ca5199&scene=21#wechat_redirect)这篇文章之前所有的分层回测都有问题。（能力有限，错误太多了，望大家见谅）
#
# 甚至，在六月份到九月份的因子，用均值合成的有一些可能是用均值的绝对值合成的。
#
# 很遗憾让大家看到了这么多错误，也很感谢大家的担待。不得不说，笔者的编程习惯确实不好，注释基本上没有。希望在接下来的一年里，能和大家一起成长。
#
# 好了，废话有点多了，进入正题。
#
# **1**
#
# **计算公式**
#
# 该因子的计算需要对过去一个月的数据进行线性回归。
#
# 回归的被解释变量是当日的收益率。
#
# 解释变量是前一日的收益率，当日收益率的方向乘当日成交量（成交额、换手率）。
#
# 然后去后者的回归系数作为因子。
#
# 需要注意的是，这里没有对收益率取绝对值，而是在成交量（成交额、换手率）的前面乘上了收益率的方向。
#
# **2**
#
# **代码**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 相关性和之前的系列因子差不多，用成交量计算的和用成交额计算的因子之间的相关性略高，而用换手率计算的因子和它们之间的相关性较低。
#
# 从IC上来看，用换手率计算的因子好一点，但是从分层回测上来看，用成交额计算的因子和用换手率计算的半斤八两。所以，在前面三部分内容将只展示用换手率计算的因子，而收益分析的时候两者都将展示。
#
# 由于这个因子在计算的时候已经使用了过去21个交易日的数据了，所以不再需要用均值或者标准差来合成月度因子了。
#
# ## IC分析
# 用成交额计算的因子只有前三年IC超过0.08，后四年只有2023年超过0.06，最后一年甚至小于0.04。
#
# ## 收益分析
# 这个换手率计算因子的分层回测可以说是好也不好。
#
# 好是因为因子值大的三组远好于因子值小的两组。
#
# 不好则是因为整个回测区间它们的多空收益都是负数。
#
# 看起来，用成交额计算的略微好一点，因为它至少有两组多空收益是正的。
#
# 好了，又到了结束的时候了，希望大家能多多点赞关注分享和推荐。
#

# ============================================================
# 本地图片清单（共 17 张）
# ============================================================
# 001.png  <-  代码
# 002.png  <-  因子评价
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  回归分析
# 010.png  <-  回归分析
# 011.png  <-  换手率分析
# 012.png  <-  换手率分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 015.png  <-  收益分析
# 016.png  <-  收益分析
# 001.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 3 个代码块）
# ============================================================

# --- 代码块 1 ---
def __call__(self):
    data = BaseDataLoader.load_data('../../data/stock_bar_1day.parquet', fields=['close', 'volume', 'turnover', 'factor'])
    self.codes = data.codes
    cap = BaseDataLoader.load_data('../../data/capital.parquet', fields=['market_cap', 'turnover_ratio'],
                                   codes=self.codes)
    rtn = (data.to_dataframe('close') * data.to_dataframe('factor')).pct_change()
    rtn_shift = rtn.shift()
    vol = data.to_dataframe('volume')
    money = data.to_dataframe('turnover')
    tr = cap.to_dataframe('turnover_ratio')
    res = Parallel(n_jobs=16, verbose=10)(
        delayed(self.cal_beta)(rtn.iloc[i - 21:i, :], rtn_shift.iloc[i-21:i], vol.iloc[i - 21:i, :],
                               money.iloc[i-21:i, :], tr.iloc[i-21:i, :])
        for i in range(23, len(rtn) + 1)
    )
    res = pd.concat(res)
    res.reset_index(inplace=True)
    res.to_parquet('./enhance_all_abs.parquet')

# (作者注) 第一段代码，主要是获取数据，然后调用cal\_beta方法计算因子。
#
# (作者注) 第6-10行，分别获取当前收益、前一日收益、成交量、成交额和换手率。
#
# (作者注) 第11-15行，调用cal\_beta方法计算因子。

# --- 代码块 2 ---
def cal_beta(self, y, x1, x2, x3, x4):
    day = y.index.tolist()[-1]
    res = []
    for code in self.codes:
        data = pd.concat([y.loc[:, code], x1.loc[:, code], x2.loc[:, code], x3.loc[:, code],
                          x4.loc[:, code]], axis=1)
        res.append(self.__cal_beta__(data.dropna()))
    res = pd.DataFrame(res, columns=['vol', 'money', 'tr'], index=self.codes)
    res['datetime'] = day
    return res

# (作者注) 按照标的遍历数据，并调用\_\_cal\_beta\_\_方法计算因子。

# --- 代码块 3 ---
@staticmethod
def __cal_beta__(data):
    y = data.iloc[:, 0].values
    if len(y) < 5:
        return [np.nan] * 3
    beta = []
    for i in range(2, 5):
        data['new'] = data.iloc[:, i] * np.sign(data.iloc[:, 0])
        x = data.iloc[:, [1, -1]].values
        x = np.c_[np.ones((len(data), 1)), x]
        beta_full = np.linalg.pinv(x.T @ x) @ x.T @ y
        beta.append(beta_full[2])
    return beta

# (作者注) 这里就是直接使用最小二乘公式计算三个不同的增强非流动性因子。
#
# (作者注) i=2，3，4分别代表成交量、成交额和换手率。
#
# (作者注) 0代表的是当日收益率，1代表的是昨日收益率。

