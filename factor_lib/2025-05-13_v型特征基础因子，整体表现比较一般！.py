"""v型特征基础因子，整体表现比较一般！

自动生成自 ../sources/zhengzhaolei/articles/2025-05-13_v型特征基础因子，整体表现比较一般！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/zhengzhaolei/articles/images/2025-05-13_v型特征基础因子，整体表现比较一般！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: zhengzhaolei
# 公众号  : 量化拯救散户
# 标题    : v型特征基础因子，整体表现比较一般！
# 日期    : 2025-05-13
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247488711&idx=1&sn=c29ce3153bfddad52b05c720b06e4185
# 本地原文: ../sources/zhengzhaolei/articles/2025-05-13_v型特征基础因子，整体表现比较一般！.md
# 本地图片: ../sources/zhengzhaolei/articles/images/2025-05-13_v型特征基础因子，整体表现比较一般！/  (共 11 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:29

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# V型特征，一般用来形容标的先跌后涨。
#
# 该因子来自《高频研究系列八—关联信息中的 Alpha》中《关联信息中的典型特征》这一小节。
#
# 该因子的计算分成了四步。
#
# 第一步，计算分钟量价幅度序列。即分钟级对数收益率与成交量占比的乘积。
#
# 第二步，计算累计分钟量价幅度序列。即符号相同的量价幅度的累加，这样会得到一个正负交替的数组。
#
# 第三步，刻画V型特征，这里的V型特征是指大幅下跌，小幅上涨，且下跌幅度大于上涨幅度的一段走势。最后，计算满足条件的相邻两个累计分钟量价幅度之和的绝对值。
#
# 第四步，得到四个因子，分别如下。
#
# negV\_mean：第三步得到的序列的均值。
#
# negV\_max：第三步得到的序列的最大值。
#
# negVwgt\_mean：第三步得到的序列除以各自对应的时间跨度后的均值。
#
# negVwgt\_max：第三步得到的序列除以各自对应的时间跨度后的最大值。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# (原文有相关段标题 [IC分析 / 回归分析 / 换手率分析 / 收益分析]，但均为图片，无独立文本；图片见下方清单)

# ============================================================
# 本地图片清单（共 11 张）
# ============================================================
# 001.png  <-  IC分析
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  回归分析
# 008.png  <-  回归分析
# 009.png  <-  换手率分析
# 010.png  <-  换手率分析
# 011.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
@staticmethod
def cal_factors(data):
    rv_cum = []
    time_cum = []
    tmp_cum = data.iloc[0]
    for i in range(1, len(data)):
        tmp_rv = data.iloc[i]
        if tmp_cum > 0 and tmp_rv > 0:
            tmp_cum += tmp_rv
        elif tmp_cum < 0 and tmp_rv < 0:
            tmp_cum += tmp_rv
        else:
            rv_cum.append(tmp_cum)
            if not time_cum:
                time_cum.append(i)
            else:
                time_cum.append(i - time_cum[-1])
            tmp_cum = tmp_rv

    rv_v = []
    rv_weight_v = []
    for i in range(1, len(rv_cum)):
        condition = rv_cum[i-1] + rv_cum[i]
        if rv_cum[i-1] < 0  and condition < 0:
            rv_v.append(np.abs(condition))
            rv_weight_v.append(rv_v[-1] / (time_cum[i-1] + time_cum[i]))
    if not rv_v:
        return [np.nan] * 4
    return [np.mean(rv_v), np.max(rv_v), np.mean(rv_weight_v), np.max(rv_weight_v)]

# (作者注) 该方法的输入为单个标的的分钟量价幅度序列组成的Series。
#
# (作者注) 第3-18行，完成第二步累计量价幅度的计算，同时计算一下每一个累计量价幅度持续的时间。
#
# (作者注) 第20-21行，完成第三步和第四步得到最后的因子。这里需要注意的是第24行，研报中有3个条件，被笔者合并称为了两个。因为rv\_cum是一个正负相间的序列，所以rv\_cum[i-1]小于0的时候，rv\_cum[i]肯定是大于0的。
#
# (作者注) 最后，展现一下评价结果。
#
# (作者注) 说实话，这四个因子的效果可谓是半斤八两，笔者标准差、均值以及均值/标准差都试过了，但是没有发现一个特别好的因子。
#
# (作者注) 所以，就简单展示一下IC绝对值最高的一个因子，negV\_max。
#
# (作者注) 这个因子用均值合成IC略高一点，用标准差合成分层回测略好一点，但是没有实质性差异。所以，分层回测就展示均值合成的了。

