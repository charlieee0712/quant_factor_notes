"""这次的非流动性因子，真的不知道该起什么名字了！

自动生成自 ../sources/zhengzhaolei/articles/2026-01-31_这次的非流动性因子，真的不知道该起什么名字了！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/zhengzhaolei/articles/images/2026-01-31_这次的非流动性因子，真的不知道该起什么名字了！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: zhengzhaolei
# 公众号  : 量化拯救散户
# 标题    : 这次的非流动性因子，真的不知道该起什么名字了！
# 日期    : 2026-01-31
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247494524&idx=1&sn=896925d110852e7be503bdf484ba3393
# 本地原文: ../sources/zhengzhaolei/articles/2026-01-31_这次的非流动性因子，真的不知道该起什么名字了！.md
# 本地图片: ../sources/zhengzhaolei/articles/images/2026-01-31_这次的非流动性因子，真的不知道该起什么名字了！/  (共 15 张)
# 段落识别: PARTIAL  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:29

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **非流动性**
#
# 可是，关于非流动性因子的研究被没有止步于此，笔者发现兴业证券的郑兆磊老师在2025年11月18日发布的研报《高频系列十一——流动性因子全解析：选股、择时与多策略》。
#
# 在这篇研报中提到的和非流动性相关的因子非常多，简单来说可以分成三类。本文，笔者和大家介绍的是第一类，该因子考虑了将每只股票和市场整体流动性进行比较。
#
# **计算步骤和代码**
#
# **该因子通过引入表征个股流动性跟随市场流动性变化的Beta系数指标，能够更好反映其在选股截面上的流动性相对情况。**
#
# 为什么这样做呢？
#
# 是因为Amihud是一个仅依赖单只股票数据计算得到的流动性因子，类似于物理学概念中的绝对速度和相对速度，其绝对数值在一定程度上缺乏好可比性。
#
# **1**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# 以标的在t时间相对于t-1时间的变化率为被解释变量，市场在t时间的流动性变化率为解释变量进行时序回归，取回归的beta作为最后的因子。
#
# 关于时间窗口，如果是1分钟的数据，则每天单独回归，然后过过去21天取均值。如果是5分钟或5分钟以上的数据，则将过去21天的数据一起进行时序回归，得到最后的因子。
#
# 关于变化率的计算，研报中使用的是对数差分。
#
# 关于市场流动性变化率，研报中取的是截面所有标的的均值。
#

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 神奇的事情出现了，之前计算的非流动性相关的因子，基于成交量、成交额或换手率计算的，相关性差距挺大的。
#
# 但是，这一次，它们之间的差距很小，如果计算的是截面斯皮尔曼相关系数的话，这三个因子的相关性都是1。
#
# 笔者也对这三个因子都进行了评价，结果发现其因子评价结果用肉眼也无法看出任何差异。
#
# 虽然，这三个因子的相关性很高，但是用它们做差并没有出现奇迹。
#
# ## IC分析
# 从IC来看，这个因子在2021年之前可以说是没啥作用的，从2022年开始勉强有那么一点点作用。
#
# ## 收益分析
# 分层回测的表现印证了之前的IC分析，多头组的收益从2022年开始超越其他组，并最终成为了整个回测区间中收益最高的一组。
#
# 不得不说，这个因子的效果并不好，甚至都没有原始的amihud非流动性因子好。
#
# 但是，它给我们带来了一种新的思路，用一个因子和截面上所有因子的均值进行时序回归，然后取beta作为因子值。
#
# 好了，关于这篇研报中第一类因子的介绍就到这里结束了，如果各位对这些内容感兴趣的话，能否点赞分享推荐和关注支持一下笔者呢？
#

# ============================================================
# 本地图片清单（共 15 张）
# ============================================================
# 001.jpg  <-  非流动性
# 002.png  <-  代码
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  回归分析
# 010.png  <-  回归分析
# 011.png  <-  换手率分析
# 012.png  <-  换手率分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 002.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 3 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]
    if idx < 243 or pd.to_datetime(date_str) >= pd.to_datetime('2026-01-01'):
        return pd.DataFrame({})
    full_path = os.path.join(self.file_pth, file_name)
    data = BaseDataLoader.load_data(full_path, fields=['volume', 'close', 'open', 'turnover'])
    rtn = data.to_dataframe('close') / data.to_dataframe('open') - 1
    vol = data.to_dataframe('volume')
    money = data.to_dataframe('turnover')
    data.to_dataframe('volume')
    tmp_cap = self.cap.iloc[idx - 243].reindex(data.codes)
    tr = vol / tmp_cap.values.reshape(1, -1)
    vol = (np.log(rtn.abs() / vol)).diff()
    money = (np.log(rtn.abs() / money)).diff()
    tr = (np.log(rtn.abs() / tr)).diff()
    res = []
    for data in [vol, money, tr]:
        res.append(self.cal_beta(data))
    res = pd.concat(res, axis=1)
    res.columns = ['vol', 'money', 'tr']
    res['datetime'] = pd.to_datetime(date_str) + timedelta(hours=15)
    return res

# (作者注) 前7行，读取数据，这里笔者的股本数据是从2005年1月到2025年12月的，而分钟行情数据是2004年1月到2026年1月的，为了避免获取不到股本数据，增加了第4-5行的保护代码。
#
# (作者注) 第8-13行，计算收益率、成交量、成交额和换手率。
#
# (作者注) 第13-15行，分别计算用换手率、成交量和成交额计算的个股流动性变换率。
#
# (作者注) 第17-19行，分别计算基于成交量、成交额和换手率的beta因子。

# --- 代码块 2 ---
def cal_beta(self, data):
    data = data.replace(np.inf, np.nan)
    data = data.replace(-np.inf, np.nan)
    cols = data.columns.tolist()
    data['x'] = data.mean(axis=1)
    beta = []
    for i in range(data.shape[1] - 1):
        beta.append(self.__cal_beta__(data.iloc[:, [i, -1]]))
    beta = pd.Series(beta, index=cols)
    return beta

# (作者注) 第2-3行，将无穷大替换为nan，这是因为对数计算的时候0会变成无穷大。
#
# (作者注) 第5行，计算市场流动性变化率。
#
# (作者注) 第6-9行，按照标的计算beta因子。

# --- 代码块 3 ---
def __cal_beta__(self, data):
    data = data.dropna()
    if len(data) <= 60:
        return np.nan
    y = data.iloc[:, 0].values.reshape(-1, 1)
    x = data.iloc[:, -1].values.reshape(-1, 1)
    x = np.c_[np.ones((len(data), 1)), x]
    beta_full = np.linalg.pinv(x.T @ x) @ x.T @ y
    return beta_full[1, 0]

# (作者注) 这部分就是计算beta因子了，需要注意的是笔者对每日有效时间小于60分钟的标的都返回nan值。

