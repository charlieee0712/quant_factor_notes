"""邪修！直到走火入魔，终于弄出来一个稍微能看的因子！

自动生成自 ../sources/general/articles/2025-11-02_邪修！直到走火入魔，终于弄出来一个稍微能看的因子！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2025-11-02_邪修！直到走火入魔，终于弄出来一个稍微能看的因子！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 邪修！直到走火入魔，终于弄出来一个稍微能看的因子！
# 日期    : 2025-11-02
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247491329&idx=1&sn=95612ffed7a912f1b3e4390b849b883b
# 本地原文: ../sources/general/articles/2025-11-02_邪修！直到走火入魔，终于弄出来一个稍微能看的因子！.md
# 本地图片: ../sources/general/articles/images/2025-11-02_邪修！直到走火入魔，终于弄出来一个稍微能看的因子！/  (共 16 张)
# 段落识别: PARTIAL  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **一阶自相关性差分**
#
# 在半年多之前，笔者对一阶自相关性的因子进行了一系列的研究，发现这个系列的因子还是不错的，IC绝对值较高，分层回测的单调性也是比较好的。
#
# 于是，笔者就想着能不能在这个东西的基础上继续研究，然后得到一个更好的因子。
#
# 不得不说，笔者最近这两天在这个方向上的研究可以说是走火入魔了。幸运的是，还是弄出来了一个能看的因子，至少能让我在周日的时候发出来一篇文章了。
#
# 关于一阶自相关性的研究，笔者在[一阶自相关性，本以为是很差的因子，没想到居然有3年IC绝对值超过0.1！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247488750&idx=1&sn=57a8ba6076a4c46d76d3a4a88540b9e8&scene=21#wechat_redirect)和[成交额一阶自相关性2.0版本，6年IC绝对值超过0.1，其中3年更是超过了0.12！](https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247489351&idx=1&sn=5fe44549d49afbda5f188c825ec6bbf9&scene=21#wechat_redirect)这两篇文章中提到过。
#
# 本文是在成交额一阶自相关性2.0版本上的又一次改进。但是，它并不是成交额一阶自相关性的3.0版本，因为笔者最后展示的因子是成交额一阶自相关性3.0版本和2.0版本的差分。
#
# **计算步骤和代码**
#
# 3.0版本的计算和2.0版本的计算类似，都用到了过去21个交易日同分钟的数据。
#
# **1**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# 第一步，判断每分钟的成交额是否大于过去21个交易日同分钟成交额的均值+1倍标准差，大于置为1，否则置为0。
#
# 第二步，计算第一步所得到的序列的一阶自相关性，作为成交额一阶自相关性的3.0版本。
#
# 第三步，计算3.0版本和2.0版本的差分。
#

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## IC分析
# 如果是用均值合成的，IC为正（这与相关性分析一致，因为2.0版本和3.0版本的IC都是负的）。其中，2023年的IC超过了0.1，超过0.08的年份有2018年和2021年。
#
# 但是，这种方式合成的因子在2018年到2025年间的表现不如标准差合成的。
#
# ## 收益分析
# 其实，这个分层回测的结果并不是很完美。毕竟，因子值处于中间三组的净值曲线交织在了一起，很难区分。
#
# 但是，它是有亮点的。因为，因子值最小的一组从2019年开始和其他的四组基本上就不存在相交的情况了。
#
# 也许，在不久的将来，这个思路能创造出不错的因子。
#

# ============================================================
# 本地图片清单（共 16 张）
# ============================================================
# 001.png  <-  (文章开头)
# 002.png  <-  相关性
# 003.png  <-  相关性
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  IC分析
# 010.png  <-  回归分析
# 011.png  <-  回归分析
# 012.png  <-  换手率分析
# 013.png  <-  换手率分析
# 014.png  <-  收益分析
# 015.png  <-  收益分析
# 016.png  <-  - *END* -

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    if idx < 20:
        return pd.DataFrame({})
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]
    amt = []
    vol = []
    for i in range(idx-20,idx+1):
        file_name = self.files[i]
        full_path = os.path.join(self.file_pth, file_name)
        data = BaseDataLoader.load_data(full_path, fields=['turnover', 'volume'])
        amt.append(data.to_dataframe('turnover'))
        vol.append(data.to_dataframe('volume'))
    amt = self.z_score(amt)
    vol = self.z_score(vol)
    res = []
    for i in range(len(amt.columns.tolist())):
        tmp_res = []
        tmp_amt = amt.iloc[:, i]
        tmp_vol = vol.iloc[:, i]
        if np.all(tmp_amt == 1) or np.all(tmp_amt == 0):
            tmp_res.append(1)
        else:
            tmp_res.append(tmp_amt.corr(tmp_amt.shift(1)))
        if np.all(tmp_vol == 1) or np.all(tmp_vol == 0):
            tmp_res.append(1)
        else:
            tmp_res.append(tmp_vol.corr(tmp_vol.shift(1)))
        tmp_res.append(tmp_res[0] - tmp_res[-1])
        res.append(tmp_res)
    res = pd.DataFrame(res, index=amt.columns.tolist(), columns=['amt', 'vol', 'diff'])
    res['datetime'] = pd.to_datetime(date_str) + timedelta(hours=15)
    return res

# (作者注) 前15行代码，读取数据，并计算完成计算步骤中第一步的计算，得到当日成交额和成交量的01序列。需要注意的是，这里用到了z\_score这个方法，这部分的代码在文章开头引用的文章中出现过，这里不赘述了。
#
# (作者注) 第17-30行，分别对成交额和成交量计算一阶自相关性。这里为什么采取了如此复杂的操作，而不是直接用corrwith，因为可能某些标的的成交额序列全是0或者全是1，这种情况用corrwith会报除0错误。所以，笔者用了这么复杂的一段代码来规避它。

