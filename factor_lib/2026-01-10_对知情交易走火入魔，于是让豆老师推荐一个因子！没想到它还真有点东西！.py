"""对知情交易走火入魔，于是让豆老师推荐一个因子！没想到它还真有点东西！

自动生成自 ../sources/general/articles/2026-01-10_对知情交易走火入魔，于是让豆老师推荐一个因子！没想到它还真有点东西！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2026-01-10_对知情交易走火入魔，于是让豆老师推荐一个因子！没想到它还真有点东西！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 对知情交易走火入魔，于是让豆老师推荐一个因子！没想到它还真有点东西！
# 日期    : 2026-01-10
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247493928&idx=1&sn=3bfe06a132d993372ae13088189d1ac5
# 本地原文: ../sources/general/articles/2026-01-10_对知情交易走火入魔，于是让豆老师推荐一个因子！没想到它还真有点东西！.md
# 本地图片: ../sources/general/articles/images/2026-01-10_对知情交易走火入魔，于是让豆老师推荐一个因子！没想到它还真有点东西！/  (共 15 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **知情交易**
#
# 自从方正证券的曹老师通过FFT变换来识别VWAP或者TWAP交易（通常也被称作知情交易者）之后，笔者似乎对通过分钟数据来寻找VWAP或者TWAP交易的痕迹走火入魔了。
#
# 但是，自己多次尝试都不太成功。
#
# 眼看着就要走火入魔了，这时候笔者想到了AI，于是让豆老师推荐一个利用分钟级别的数据来识别知情交易者参与的概率。
#
# 于是，就有了这个因子。豆老师表示，噪声交易者主导的标的，其换手率和波动率的相关性很低，而知情交易者主导的市场换手率和波动率的相关性会变高，甚至可能超过0.7。
#
# **代码**
#
# 在这种情况下，就有了本文的因子。
#
# 没错，就是换手率和波动率的相关性，豆老师推荐是计算滚动60分钟的换手率和波动率的相关性。
#
# 这一次，笔者没听劝，而是分别计算了滚动5分钟、滚动30分钟和滚动60分钟的相关性。
#
# **1**
#
# **代码**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 从相关性来看，这三个因子的相关性很高。其实，这在复现之前就是已知的。
#
# 从IC来看，频率越低，因子值的IC越低。不过，这三者之间的分层回测结果是差不多的。所以，下面将展示corr\_5这个因子的因子评价结果。
#
# 至于用均值合成月度因子还是用标准差合成，笔者测试下来的结果是均值合成的因子全方位领先。
#
# ## IC分析
# 这个因子和corr\_30比，IC好像没什么太大的差距。
#
# corr\_30只有一年的绝对值超过了0.1，但是它有六年都超过了0.08；而corr\_5则在2019年离0.08差了一点点。
#
# ## 收益分析
# 从分层回测来看，这个因子还行也不太行。
#
# 说它还行是因为，因子值最大的两组相对于中间组收益都是落后的，从第二张图中可以明显的看出（收益率减去group\_2的收益率），绿线和红线都是一路向下。同时，从2022年开始，它的分层回测也表现出了单调性。
#
# 说它不行，是因为从2018年到2022年，因子值最小两组收益率都不如中间组。
#
# 说实话，这个因子有缺陷，但是笔者还是感到满意的。至少，没有IC和分层双杀。
#
# 最后，又到了求赞求分享求推荐求关注的时间了，希望各位大佬能动动金手支持一下笔者，你们每一次的支持都是笔者更新的动力。
#

# ============================================================
# 本地图片清单（共 15 张）
# ============================================================
# 001.png  <-  代码
# 002.png  <-  因子评价
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  回归分析
# 010.png  <-  回归分析
# 011.png  <-  换手率分析
# 012.png  <-  换手率分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 001.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 2 个代码块）
# ============================================================

# --- 代码块 1 ---
def __init__(self, file_pth, name, need_last_close, gap=100, parallel="threading"):
    super().__init__(file_pth, name, need_last_close, gap, parallel)
    cap = BaseDataLoader.load_data('../../data/capital.parquet', fields=['circulating_cap'])
    self.cap = cap.to_dataframe('circulating_cap')

# (作者注) 第一段代码，是在初始化的时候读取了一下流通股本的数据，方便计算滚动n分钟的换手率。

# --- 代码块 2 ---
def process_single_day(self, idx):
    # 加载当日分钟数据
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]
    cur = pd.to_datetime(date_str) + timedelta(hours=15)
    if idx < 243:
        return pd.DataFrame(columns=['corr_5', 'corr_30', 'corr_60', 'datetime'])
    full_path = os.path.join(self.file_pth, file_name)
    data = BaseDataLoader.load_data(full_path, fields=['volume', 'close', 'open'])
    cap = self.cap.iloc[idx - 243].reindex(data.codes)
    cap = cap.values.reshape(1, -1)
    rtn = data.to_dataframe('close') / data.to_dataframe('open') - 1
    vol = data.to_dataframe('volume')
    res = []
    for i in [5, 30, 60]:
        res.append((vol.rolling(i).sum() / cap).corrwith(rtn.rolling(i).std()))
    res = pd.concat(res, axis=1)
    res.columns = ['corr_5', 'corr_30', 'corr_60']
    res['datetime'] = cur
    return res

# (作者注) 这段代码也很简单，唯一需要注意的是第6-7行。
#
# (作者注) 因为笔者的日频数据是从2005年开始的，而分钟数据是从2004年开始的，这两个数据之间有个gap。通过观察数据，知道了2004年有243个交易日，那么2005年的第一个交易日就是从索引243开始。所以，当索引小于243时，直接返回空的数据。
#
# (作者注) 其他的代码就不需要什么特别的说明了。

