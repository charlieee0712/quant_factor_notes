"""同价成交量分布中alpha因子，IC和分层回测都还不错

自动生成自 ../sources/zhengzhaolei/articles/2025-04-30_同价成交量分布中alpha因子，IC和分层回测都还不错.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/zhengzhaolei/articles/images/2025-04-30_同价成交量分布中alpha因子，IC和分层回测都还不错/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: zhengzhaolei
# 公众号  : 量化拯救散户
# 标题    : 同价成交量分布中alpha因子，IC和分层回测都还不错
# 日期    : 2025-04-30
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247488301&idx=1&sn=e9338199d3b01c6c34cdc5b5b38b87f3
# 本地原文: ../sources/zhengzhaolei/articles/2025-04-30_同价成交量分布中alpha因子，IC和分层回测都还不错.md
# 本地图片: ../sources/zhengzhaolei/articles/images/2025-04-30_同价成交量分布中alpha因子，IC和分层回测都还不错/  (共 22 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T03:24:29

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# 随着协同价差因子复现完之后，关于笔者拥有的数据能复现的曹春晓老师的因子，已经全部复现完了。
#
# 接下来，笔者将会进入到兴证金工首席分析师郑兆磊老师的研报复现中。
#
# 在之前，笔者复现过两个系列了。这里需要注意的是，郑老师的一篇研报中往往会存在多个类型的因子，所以笔者会对其进行拆解。
#
# 本文介绍的因子来自2022年8月29日的研报，《高频研究系列四——成交量分布中的Alpha》。从目录来看，该研报一共介绍了三种成交量分布中的Alpha因子，包括常见成交量分布因子、异构成交量分布因子和同价成交量分布因子。
#
# 本文要复现的就是第三种，同价成交量分布因子。
#
# 研报中，对同价成交量的分布类型进行了分析，一共有三种b型、p型和D型。b型意味着成交量集中在高价区间，b 型轮廓的成交量分布通常出现在下跌趋势中或上涨趋势结束时，个股预期下跌。p型意味着成交量集中在低价区间，P 型轮廓的成交量分布通常出现在上涨趋势中或下跌趋势结束时，个股预期上涨。D型意味着价格处于平衡状态，当个股的同价成交量分布出现 D 型时，其整体处于相对平衡的状态，未来走势不明朗。
#
# 笔者将用文字简单介绍一下这一系列因子的构建过程和具体因子的计算方法。
#
# 首先，对收盘价相同的成交量求和，得到同价成交量，并将成交量累积最大的价格作为当日的支撑点。
#
# 然后，以支撑点为中心，计算它周围的成交量累计值，当累计值超过当日成交量的50%后结束，此时将区域价格上限记作vsa\_high，区域价格下限记作vsa\_low。这里需要注意，计算累积值的时候按照价格由近及远，成交量由大及小的规则来计算。
#
# 最后计算三个因子。
#
# vsa\_ratio，下界与日度收盘价的比值。
#
# vsa\_low2max，下界与日内最高收盘价的比值。若上限价格与当日最低价越接近，说明成交量支撑区域越接近日内的低价区域，则该股日内的成交量分布越接近于b 型成交量分布，预期下涨；当个股价格出现急剧下跌然后盘整，或者短期急剧上涨但整体处于低位时，成交量分布将出现b型曲线。
#
# vsa\_high2min，上界与日内最低收盘价的比值。若下限价格与当日最高价越接近，说明成交量支撑区域越接近日内的高价区域，则该股日内的成交量分布越接近于p 型成交量分布，预期上涨；当个股价格出现急剧上涨然后盘整，或短期急剧下跌但日内整体处于高位时，通常会出现P型成交量曲线。
#
# 接下来，将通过代码来详细介绍一下。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 收益分析
# vsa\_low2max
#
# 这个因子和前面一个相比，IC和分层回测的表现都没有实质性差异，所以就不展示了。
#
# vsa\_high2min
#
# 为了和上面的对比，该因子也是用均值/标准差合成的。
#
# ## 收益分析
# 总结：vsa\_high2min因子在IC上表现更好，但是分层回测就不如vsa\_ratio了。
#

# ============================================================
# 本地图片清单（共 22 张）
# ============================================================
# 001.png  <-  IC分析
# 002.png  <-  IC分析
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  回归分析
# 008.png  <-  回归分析
# 009.png  <-  换手率分析
# 010.png  <-  换手率分析
# 011.png  <-  收益分析
# 012.png  <-  IC分析
# 013.png  <-  IC分析
# 014.png  <-  IC分析
# 015.png  <-  IC分析
# 016.png  <-  IC分析
# 017.png  <-  IC分析
# 018.png  <-  回归分析
# 019.png  <-  回归分析
# 020.png  <-  换手率分析
# 021.png  <-  换手率分析
# 022.png  <-  收益分析

# ============================================================
# 作者代码（按原文出现顺序，共 2 个代码块）
# ============================================================

# --- 代码块 1 ---
def process_single_day(self, idx):
    file_name = self.files[idx]
    date_str = file_name.split('.')[0]

    # 加载当日分钟数据
    full_path = os.path.join(self.file_pth, file_name)
    data = BaseDataLoader.load_data(full_path, fields=['close', 'volume', 'turnover', 'low', 'high'])
    res = []
    for i in range(len(data.codes)):
        tmp_data = pd.DataFrame(data=data.data[:, 0:3, i], columns=['close', 'volume', 'turnover'])
        res.append(self.cal_factors(tmp_data))

    res = pd.DataFrame(data=res, index=data.codes.tolist(), columns=['vsa_high2min', 'vsa_low2max',
                                                                     'vsa_ratio', 'smart'])
    res['datetime'] = pd.to_datetime(date_str) + timedelta(hours=15)
    res.index.name = 'code'
    return res.reset_index()

# (作者注) 这一块比较简单，主要是加载数据，然后调用cal\_factors方法计算因子。

# --- 代码块 2 ---
@staticmethod
def cal_factors(data):
    close = data.iloc[-1, 0]
    high = np.nanmax(data.iloc[:, 0])
    low = np.nanmin(data.iloc[:, 0])

    data = data.groupby('close').sum()
    data.reset_index(inplace=True)

    vol = data['volume'].sum()
    vol_thd50 = vol * 0.5

    idx = np.argmax(data['volume'])
    data['diff'] = np.abs(data['close'] - data.iloc[idx, 0])
    data.sort_values(by=['diff', 'volume'], ascending=[True, False], inplace=True)
    
    cur_vol = 0
    for i in range(len(data)):
        cur_vol += data.iloc[i, 1]
        if cur_vol >= vol_thd50:
            data = data.iloc[:i + 1]
            break
    vsa_high = data['close'].max()
    vsa_low = data['close'].min()
    return [vsa_high / low, vsa_low / high, vsa_low / close]

# (作者注) 第3-5行，获取当日收盘价，最高收盘价和最低收盘价，用于最后的因子计算。
#
# (作者注) 第7-8行，计算同价成交量。
#
# (作者注) 第10-11行，计算成交量门限。
#
# (作者注) 第13行，计算累积成交量最大的收盘价。
#
# (作者注) 第14行，计算所有价格和累积成交量最大的收盘价的差分。
#
# (作者注) 第15行，对数据按照价格差分升序，成交量降序排列。
#
# (作者注) 第17-22行，获取成交量占比达到50%的区域。
#
# (作者注) 最后，计算因子。
#
# (作者注) vsa\_ratio
#
# (作者注) 这个因子用标准差合成IC更高，分层回测的结果两者半斤八两。但是，均值和标准差的IC方向是相反的。于是，笔者就尝试了一下用均值/标准差，然后发现IC没有比标准差差，而且分层回测变好了一点，所以就展示这个的评价结果了。

