"""活动筹码因子，IC表现好于筹码分布！

自动生成自 ../sources/general/articles/2026-06-16_活动筹码因子，IC表现好于筹码分布！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2026-06-16_活动筹码因子，IC表现好于筹码分布！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 活动筹码因子，IC表现好于筹码分布！
# 日期    : 2026-06-16
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247497363&idx=1&sn=e59ffd18cb7cd474d14aa2a29d7319fe
# 本地原文: ../sources/general/articles/2026-06-16_活动筹码因子，IC表现好于筹码分布！.md
# 本地图片: ../sources/general/articles/images/2026-06-16_活动筹码因子，IC表现好于筹码分布！/  (共 16 张)
# 段落识别: SKELETON_ONLY  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# **活动筹码**
#
# 本文，笔者将继续回到中信建投陈升锐和姚紫薇两位老师在2025年8月25日发布的研报《筹码分布因子系统构建》。
#
# 在此之前，笔者复现了这篇研报中的筹码分布和筹码集中度两个系列的因子，其中表现最好的当属筹码分布系列中的偏度因子。
#
# 本文，笔者将复现一下这篇研报中与活动筹码相关的五个因子。
#
# 在正式开始介绍这系列因子之前，笔者要先感谢一下大家。因为大家的支持，笔者可能又要接到新的广子了，应该还是课程类的，具体内容还没看。希望各位大佬不要因为笔者接了广子就取关了，笔者还需要各位大佬们的继续支持。
#
# 也希望这篇文章的阅读能超过广子，哈哈哈！
#
# **计算公式和代码**
#
# 什么是活动筹码呢？
#
# 根据研报的描述，当天涨跌停范围内的价格筹码更可能被主动交易，成为“活动筹码”；而处于该区间之外的筹码则为“锁定筹码”，通常更稳定，不易交易。
#
# **1**
#
# **计算公式**
#
# 研报中，需要根据每个标的所属的市场计算高位价格和低位价格，然后再计算因子。其中，高位价格=收盘价\*（1+涨跌停幅度），低位价格=收盘价\*（1-涨跌停幅度）。
#
# 这里，笔者直接拿了当天的limit\_up作为高位价格，limit\_down作为低位价格。
#
# 活动筹码ASR=高位价格与低位价格之间的筹码占比。
#
# 上方活动筹码ASR\_upper=收盘价和高位价格之间的筹码占比。
#
# 下方活动筹码ASR\_lower=收盘价和低位价格之间的筹码占比。
#
# 上方锁定筹码ASR\_above=高位价格上方的筹码占比。
#
# 下方锁定筹码ASR\_below=低位价格下方的筹码占比。
#
# **2**
#
# **代码**
#
# 整个代码的核心框架和之前两篇文章是一致的，只是计算因子的公式有所变化。

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 从相关性来看，三个活动筹码之间的相关性还是挺高的，最低的也超过了0.7。
#
# 而两个锁定筹码之间的相关性就有点“玄学”了，把比如上方锁定筹码和下方活动筹码之间的相关性的绝对值竟然超过了0.9，而下方锁定筹码和其他四个因子的相关性都不是太高。
#
# 在这五个因子中，用过去21个交易日的均值低频化的上方活动筹码在分层回测上的表现是最好的，因此接下来将仅展示这个因子的因子评价结果。
#
# ## IC分析
# 这个IC如果放在笔者复现过的一系列因子中，它的表现不值得一提。但是，在这篇研报中，看起来似乎是表现最好的了。
#
# ## 收益分析
# 这个分层回测的单调性并不是很完美，红线的净值超过了绿线，甚至有些阶段还超过了黄线。
#
# 但是，其因子值最小的一组在大部分时间里面都是领先其他组的，而因子值最大的一组则落后其他组。
#
# ## 总结
# 活动筹码因子从IC上来看，是好于筹码分布和筹码集中度的。
#
# 但是，笔者感觉这个分层回测的结果却是不如筹码分布中的偏度因子的。
#
# 这篇研报后续还有两个系列的因子，笔者也将一一复现，希望各位大佬能够喜欢。在喜欢的同时，也能通过点赞关注推荐和分享的方式来支持支持笔者，谢谢大家。
#

# ============================================================
# 本地图片清单（共 16 张）
# ============================================================
# 001.png  <-  代码
# 002.png  <-  因子评价
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  回归分析
# 010.png  <-  回归分析
# 011.png  <-  换手率分析
# 012.png  <-  换手率分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 015.png  <-  收益分析
# 016.png  <-  总结

# ============================================================
# 作者代码（按原文出现顺序，共 1 个代码块）
# ============================================================

# --- 代码块 1 ---
def cal_factors(self, code, close, volume, factor, tr, high_price, low_price):
    data = pd.concat([close, volume], axis=1)
    data.columns = ['close', 'volume']
    data = data.groupby('close', as_index=False, group_keys=False).sum()
    last_data = self.last_chip.get(code, None)
    if last_data is None:
        data.iloc[:, -1] *= tr
    else:
        last_data['close'] /= factor
        data = data.merge(last_data, how='outer', on=['close'])
        data = data.fillna(0)
        data['volume'] = data.iloc[:, -1] * (1 - tr) + data.iloc[:, -2] * tr
        data = data[['close', 'volume']]
    tot_chip = data.iloc[:, 1].sum()
    asr = data[(data['close'] >= low_price) & (data['close'] < high_price)].iloc[:, 1].sum() / tot_chip
    asr_upper = data[(data['close'] >= close) & (data['close'] < high_price)].iloc[:, 1].sum() / tot_chip
    asr_lower = data[(data['close'] >= low_price) & (data['close'] < close)].iloc[:, 1].sum() / tot_chip
    asr_above = data[data['close'] >= high_price].iloc[:, 1].sum() / tot_chip
    asr_below = data[data['close'] < low_price].iloc[:, 1].sum() / tot_chip
    data.iloc[:, 0] *= factor
    self.last_chip[code] = data.copy()
    return asr, asr_upper, asr_lower, asr_above, asr_below

# (作者注) 这段代码的主要改动就是传入了high\_price和low\_price两个值。
#
# (作者注) 第14-19行，就是根据公式来计算因子值，其他的代码都和之前的两篇文章中的代码是一模一样的，这里就不多费口舌了

