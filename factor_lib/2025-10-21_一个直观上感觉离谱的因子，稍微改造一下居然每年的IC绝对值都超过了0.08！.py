"""一个直观上感觉离谱的因子，稍微改造一下居然每年的IC绝对值都超过了0.08！

自动生成自 ../sources/general/articles/2025-10-21_一个直观上感觉离谱的因子，稍微改造一下居然每年的IC绝对值都超过了0.08！.md。
所有 # 注释内容均直接摘录自原文，未经改写或归纳。
图片位于 ../sources/general/articles/images/2025-10-21_一个直观上感觉离谱的因子，稍微改造一下居然每年的IC绝对值都超过了0.08！/
"""

# ============================================================
# 来源
# ============================================================
# 来源标识: general
# 公众号  : 量化拯救散户
# 标题    : 一个直观上感觉离谱的因子，稍微改造一下居然每年的IC绝对值都超过了0.08！
# 日期    : 2025-10-21
# 原文 URL: https://mp.weixin.qq.com/s?__biz=MzkwNjYzMTEyMg==&mid=2247490984&idx=1&sn=79d344fe276593300a37827881846fa4
# 本地原文: ../sources/general/articles/2025-10-21_一个直观上感觉离谱的因子，稍微改造一下居然每年的IC绝对值都超过了0.08！.md
# 本地图片: ../sources/general/articles/images/2025-10-21_一个直观上感觉离谱的因子，稍微改造一下居然每年的IC绝对值都超过了0.08！/  (共 15 张)
# 段落识别: PARTIAL  (FULL=三段齐备 / PARTIAL=部分 / SKELETON_ONLY=仅回测段 / NONE=无段标题)
# 生成时间: 2026-06-22T00:56:51

# ============================================================
# 导读（原文头部，至首个内容段标题或首个代码块前）
# ============================================================
# 本文复现的因子来自招商证券任曈老师在2323年发布的研报《基于股票代码有序性的“注意力溢出”》。
#
# 在开始正文之前，笔者想要道个歉。这篇研报笔者在2024年的上半年的时候在《因子日历》上看到了它，但是一直没有复现它。
#
# 因为，这个因子用到了股票代码排序。笔者第一眼看到就觉得这个因子很离谱，效果应该不好。
#
# 可是，随着时间的推移，笔者发现《因子日历》上的因子大部分都是非常不错的，所以这次决定看看这个因子到底怎么样。
#
# 《因子日历》对这个因子给出的说明是这样的：
#
# > 由于投资者注意力有限，以及行情软件通常是根据代码顺序依次展示股票信息，投资者会轻易地关注到与焦点股票相邻的其他股票，出现“注意力溢出”，焦点股票与相邻股票之间会存在价格与交易情绪的转移；“自信溢出”对投资者因近期获利的过度自信向邻近股票外溢的情况，与未来收益正相关。
#
# **计算步骤及代码**
#
# 我们先用文字来简单描述一下这个因子的计算步骤，这样你们可能就会理解为啥笔者第一次看到这个因子的时候会觉得它离谱了。
#
# **1**

# ============================================================
# 作者原文 — 计算步骤
# ============================================================
# 第一步，将股票按代码从小到大进行排序后，对每只股票计算相邻n只股票在过去T日的日均涨跌幅，并等权合成为焦点股票i的NBR\_ret。
#
# 第二步，将焦点股票i的NBR\_ret对其自身的日军涨跌幅进行截面回归，回归得到的残差即为“自信溢出”因子RNBR\_ret。
#
# 注意：焦点股票i的相邻股票数对因子效果影响不大，建议使用较少数量的股票，比如10。
#
# 上面的是《因子日历》中介绍的因子计算步骤，用这个计算出来的因子比较一般，只有3年的IC绝对值超过了0.06。
#
# 于是，笔者给它改造了一下，将第一步中的过去T日改为过去1日，然后每天做截面回归，得到残差，然后用过去21个交易日的残差因子合成月度因子。
#

# ============================================================
# 作者原文 — 因子逻辑
# ============================================================
# (原文中无此段落)

# ============================================================
# 作者原文 — 回测表现说明
# ============================================================
# ## 因子评价
# 由于这个因子计算了之后得到的是日频的因子，根据咱们之前的套路，还需要用过去21个交易日的均值或者方差来合成月度的因子。
#
# 无论是用均值合成还是用标准差合成，这个改进后的因子的表现都非常不错，累积IC接近-8。
#
# 不过，用标准差合成的因子IC绝对值更高，所以笔者接下来的因子评价结果展示的也是用标准差合成的。
#
# ## 收益分析
# 虽然，这个因子在IC上的表现是不错的（每一年的IC绝对值都超过了0.08，其中有三年的IC绝对值甚至超过了0.1），但是其在分层回测上的表现有点一般。因子值最大的一组收益率最低，而因子值最小的一组收益率却不是最高的。
#

# ============================================================
# 本地图片清单（共 15 张）
# ============================================================
# 001.png  <-  (文章开头)
# 002.png  <-  代码
# 003.png  <-  IC分析
# 004.png  <-  IC分析
# 005.png  <-  IC分析
# 006.png  <-  IC分析
# 007.png  <-  IC分析
# 008.png  <-  IC分析
# 009.png  <-  回归分析
# 010.png  <-  回归分析
# 011.png  <-  换手率分析
# 012.png  <-  换手率分析
# 013.png  <-  收益分析
# 014.png  <-  收益分析
# 015.png  <-  - *END* -

# ============================================================
# 作者代码（按原文出现顺序，共 2 个代码块）
# ============================================================

# --- 代码块 1 ---
def __call__(self):
    data = BaseDataLoader.load_data('../../data/stock_bar_1day.parquet', fields=['close', 'factor', 'volume']
                                         ).to_dataframes()
    close = data['close'] * data['factor']
    vol = data['volume'] != 0
    close = close * np.where(vol, 1.0, np.nan)
    self.rtn = close.pct_change()
    res = []
    for i in tqdm(range(1, len(self.rtn))):
        res.append(self.process_single_day(i))
    res = pd.concat(res, axis=1).T
    res.index = self.rtn.index.tolist()[1:]
    res.index.name = 'datetime'
    res.reset_index(inplace=True)
    res = pd.melt(res, id_vars='datetime', var_name='code', value_name='rnbr_ret')
    res.to_parquet('./rnbr_ret.parquet')

# (作者注) 第一段代码，主要是加载数据并对数据进行预处理。
#
# (作者注) 第4行，用每日收盘价乘上每日的复权因子，得到股权的价格。
#
# (作者注) 第5-6行，通过成交量是否为0来判断是否停牌，并将其置为nan。
#
# (作者注) 第7行，用pct\_change来计算价格的变换，也就是收益率。
#
# (作者注) 第8-10行，调用process\_single\_day来计算每日的因子。

# --- 代码块 2 ---
def process_single_day(self, idx):
    rtn = self.rtn.iloc[idx].dropna()
    rtn.sort_index(inplace=True)
    x = [rtn.iloc[:10].mean()] * 5
    for i in range(5, len(rtn) - 5):
        x.append(rtn.iloc[i-4:i+6].mean())
    x += [rtn.iloc[-10:].mean()] * 5
    x = np.array(x).reshape(-1, 1)
    y = rtn.values.reshape(-1, 1)
    lr = LinearRegression()
    lr.fit(x, y)
    resi = y - lr.predict(x)
    return pd.Series(resi.reshape(-1), index=rtn.index.tolist())

# (作者注) 第2行，去掉收益率为nan的标的。
#
# (作者注) 第3行，对数据按照代码从小到大排序。
#
# (作者注) 第4-7行，计算第一步中的NBR\_ret。这里，前5个标的的NBR\_ret为前10个标的收益率的均值，最后5个标的的NBR\_ret为最后10个标的收益率的均值。
#
# (作者注) 剩下的就是线性回归然后计算残差了。

